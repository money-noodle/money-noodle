# Decision archive — 2026-08-24 through 26

> **Status:** Immutable historical archive · **Index:** [`../decision-log.md`](../decision-log.md)  
> **Specification root:** [`../../SPEC.md`](../../SPEC.md)  
> **Entries:** 14
>
> These entries were moved without changing their decision text. Relative links were adjusted only for the
> archive depth. Do not rewrite or reorder historical decisions; supersede them with a new current decision.

| Date | Decision |
|---|---|
| 2026-08-26 | Retire `long-shot-round-trip-buy12-sell97-win600-v2` after its frozen prospective paper cohort reached 150 resolved attempts across 76 independent settlement windows. Exact P&L was −1,410.93¢ on 4,979¢ staked; hold was −14.71% ±27.56pp clustered SE, and the precommitted paired 97¢ exit-minus-hold result was −2.47pp ±0.85pp / −98.93¢, with all 11 exited positions later settling in the owned side. Remove long-shot paper/live execution, quote/trailing/target pollers, evidence writes, dashboard/API, Postgres replication, funding and estimation commands, and the strategy-level allocation layer. Retain the applied migration, durable evidence, and `long-shot-round-trip` as a retired ledger identity so historical accounting cannot enter the edge policy. Provider → market remains the active allocation chain; no former strategy share raises an edge limit, and no edge policy or capital rule changes. See `reports/long-shot-v2-final-review-2026-08-26.md`. |
| 2026-08-25 | Clarify adaptive-regime presentation without changing its arithmetic, thresholds, state transitions, or authority. Name `negativeReturnConfidence` on user surfaces as **estimated negative-return probability**; state explicitly that an open gate permits entries and that pausing requires the configured probability threshold; label `weightedMeanEdge` as weighted recent fee-aware return; and show resolved-window count and warm-up minimum as separate quantities rather than a fraction that resembles capacity. Historical transition reasons remain immutable. This is presentation-only: production v22, the 99% pause/75% resume thresholds, 12-window warm-up, half-life, evidence, and every order path are unchanged. |
| 2026-08-25 | Replace every hardcoded Kalshi event-order `exchange_index` with the exact venue-owned index from a signed active-market read immediately before submission. Validate exact ticker, active status, and non-negative safe-integer index without coercion or fallback; capture one index across maker create retries, require every management refresh to retain it, and inject it into maker create/amend, taker entry, and fresh-identity reduce-only exit bodies. Persist accepted entry/exit indexes as audit identity, but give them no policy, budget, reconciliation, or settlement authority. Missing, malformed, changed, mismatched, or inactive identity fails before another wire request; create transport uncertainty and reconciliation remain unchanged. This repairs 15 consecutive `market_not_found` creates after active contracts moved from the hardcoded index 0 to venue-reported index 2, but index 2 is evidence—not a new constant. See `docs/kalshi-exchange-index-wire-design.md`. |
| 2026-08-25 | Narrow current Kalshi position ownership to local lifecycle rows that can still own exposure: `open`, `pending_reservation`, `uncertain`, or exit-pending before contract close. A rejected, unfilled, sold, won, lost, or invalid local attempt no longer claims an exact ticker merely because its close is in the future, so acceptable external positions cannot hold the entire desk suspended after authoritative local rejection. Preserve fail-closed comparison while any local exposure is open or unresolved; do not infer offsets, import external orders/fills into the ledger, permit unrelated resting orders, or change cash, reservation, exposure, identity, exit, startup/Resume, or guarded-recovery controls. Simultaneous external and Money Noodle ownership of one exact ticker remains unsupported and blocking. See `docs/external-venue-position-ownership-design.md`. |
| 2026-08-25 | Approve a four-phase paper-execution fidelity workstream without changing funded execution or advancing the seven-layer serial roadmap. F1 repairs the dormant `queueClearFraction` ownership so it applies whenever paper joins a displayed queue—initially, after missing-depth recovery, and after a price-changing amendment—while the active neutral value proves exact complete-simulation parity and keeps v6. F2 prospectively shadows the sub-second create/acknowledgement race and one approximately three-second read-after-horizon grace; the executable horizon remains 12 seconds, only venue-event-time prints inside that horizon qualify, and late prints replay against the limit effective at event time. F3 scores one frozen queue-clear family only on exact accepted same-route/same-quantity makers with corrected multiple comparisons. F4 validates at most one combined generation over 300 exact-pair windows and 100 divergent outcomes with fidelity, isolation, and independent exact/whole-cent accounting gates. Optional public reads are capped and fail-drop; no candidate reads a live outcome to set paper, extends execution, rewrites history, optimizes for return, or auto-adopts. Freeze the retained/adopted paper generation before any serial execution-evidence cohort. See `docs/paper-execution-fidelity-v2-design.md`. |
| 2026-08-25 | Queue attempt-and-outcome evaluation strictly after the live-authorization final review. Keep the shared execution and budget ledgers authoritative; add only a detached append-only projection that separates intent, venue-order, position, and cash states and types reservation refusal, pre-submit refusal, create rejection, accepted maker/IOC zero-fill, partial/full fill, uncertainty/recovery, sale, settlement, and invalidity. Classify identity/ownership/reservation/cancellation/uncertainty/reconciliation/reduce-only controls as safety; exact-versus-whole-cent and idempotent reserve/release/settle behavior as accounting; maker management, episode rearming, fresh-quote cancellation, and other post-authorization choices as economic lifecycle; and the funnel, latency, depth, fill selection, provenance, and implementation shortfall as diagnostics. Require pure grid plus 25 terminal controls, 10-window wiring, 100 closed authorized-intent windows at 95% diagnostic and 100% critical ownership/money coverage, the complete fault matrix plus 1,000 seeded schedules per critical submit/accept/fill/cancel/reconcile/settle race, then 300 closed windows and 100 nontrivial paths or documented insufficient activity. Only a new amendment may freeze one mutually exclusive safety/accounting or economic generation; require its separate 300-window and seven-day or 200-execution/100-divergence gate before one final review. Counts never promote, rare production faults are never manufactured, and any schema/lifecycle change remains a separate manual implementation. See `docs/attempt-outcome-evaluation-design.md` and `docs/forecast-model-and-evaluator-v3-design.md` §§13–14. |
| 2026-08-25 | Queue live-authorization evaluation strictly after the portfolio-selection final review. Preserve `live-skip-v1` as a first-blocker episode journal and add a separate detached manifest for every portfolio-selected exact candidate, including no-intent outcomes and every simultaneous pass/fail/unavailable result. Classify worker/environment/operator/provider/reconciliation/identity/reservation/wire/uncertainty/drain controls as safety authority; classify stake, funding, exposure, filled-order, drawdown, and lifetime-loss limits as capital/risk ceilings; retain regime, asset, persistence, retry, spread, route, and switch economics with their existing owners. Stamp exact contract/order identity, control/provider/budget/reconciliation generations, ledger fingerprint, clocks, authority age, and captured cents ceiling without secrets. Require pure grid plus 25 exact controls, 10-window wiring, 100 closed windows at 95% manifest coverage and 100% issued-intent/reservation coverage, a complete fault matrix plus 1,000 seeded schedules per critical pause/reconciliation/reservation race, then 300 closed windows and 100 independent authority differences or documented inertness. Only a new amendment may freeze one fail-closed repair/fencing/lease/funding generation; require 300 closed windows, 100 authorization opportunities, complete intent coverage, and seven continuous reconciliation-cadence days before one final review. Safety correctness does not require higher return; no review may loosen capital or authority automatically. See `docs/live-authorization-evaluation-design.md` and `docs/forecast-model-and-evaluator-v3-design.md` §§12, 14. |
| 2026-08-25 | Queue portfolio-selection evaluation strictly after the venue-candidate final review. Keep `portfolio-choice-set-v1` unchanged as the conditional issued-order integrity sentinel and start a separate generation for every fresh calculation with a venue candidate, including no-order outcomes. Classify global position, same-asset/window, same-window, same-group, account-wide, and per-placement ceilings as safety/risk controls; classify expected-profit ranking, static groups/cent penalties, positivity, greedy construction, direction/size blindness, and immediate capacity use as economic selectors; keep exact subset optimum, objective gap, active/inert bind rates, downstream unused capacity, and side/quantity/stake-aware exposure diagnostic during attribution. Require fault/tie/boundary grid plus 25 exact-control observations, 10-window wiring, 100 closed windows at 95% complete decision/outcome coverage and complete issued-intent coverage, then 300 closed windows plus 100 solver/selector/rerank differences or documented inertness. Only a new amendment may freeze one small family; every arm maintains its own causal shadow portfolio, followed by 300 signal/200 execution-scoreable/100 divergent windows and one corrected review. Hindsight best-of-window cannot be a candidate, and no result may loosen a hard ceiling without separate capital/downside approval. See `docs/portfolio-selection-evaluation-design.md` and `docs/forecast-model-and-evaluator-v3-design.md` §§11, 14. |
| 2026-08-25 | Queue venue-candidate evaluation strictly after the confirmed-signal final review. Ask which exact-provider checks independently select a cohort and whether displayed value survives construction, exact pre-submit refresh, submitted limit, fill/no-fill, fees, exit, and settlement. Classify exact provider/variant/contract provenance, side/close alignment, quote validity/lattice/freshness, quantity step, all-in reserve, and unsupported/missing evidence as safety or mechanical feasibility; classify the 10c spread ceiling and any tighter quote-age/implementation-shortfall rule as economic; keep movement, depth, fill estimates, outcomes, and all simultaneous refusal reasons diagnostic during attribution. Treat 0.3×/30pp sizing as capital, provider cash as live authorization, position constraints as portfolio, cooldown/episodes as execution lifecycle, and maker/IOC as execution style—not venue attractiveness. Current single-funded-provider net-edge ranking supplies no multi-provider evidence. Require fault-grid/25-observation parity, 10/100/300 attribution milestones with 95% exact-provider coverage and 100 selector-changing windows or documented inertness, then freeze a new-outcome family before a separate 300-signal/200-execution/100-divergent cohort and one corrected review. No threshold selected from attribution may use that cohort as promotion evidence. See `docs/venue-candidate-evaluation-design.md` and `docs/forecast-model-and-evaluator-v3-design.md` §§10, 14. |
| 2026-08-25 | Queue confirmed-signal evaluation strictly after the base-signal final review; its clock does not start at the current three-day forecast-candidate checkpoint. Freeze one exact production control plus exact-provider two-consecutive/15s, first-pass, three-consecutive/30s, and current-plus-one-of-prior-two arms. Require current pass in every firing arm; tolerate only one genuine available economic failure in the noise-tolerant arm, never unavailable, stale, malformed, duplicate, or target-mismatched evidence. Key state and outcomes by strategy, market, provider contract/provenance, asset, side, and UTC close; cross-provider prices are diagnostic unless separately approved as a forecast-model input. Hold warm-up, cutoff, freshness, base policy, fees, and first-entry-episode scope fixed. Classify every condition as economic, safety, or diagnostic; economic selectors must change and beat production, safety controls use fault/invariant evidence, and diagnostics cannot authorize or refuse. Require exact-control grid/25-observation parity, 10/100/300 signal milestones with 95% exact-outcome and 90% per-arm availability bars plus 100 divergent windows, then 200 execution-scoreable/100 divergent windows and one Holm-corrected review. Counts never promote automatically. See `docs/confirmed-signal-evaluation-design.md` and `docs/forecast-model-and-evaluator-v3-design.md` §§9, 14. |
| 2026-08-25 | Activate Phase 2 `forecast-candidate-registry-v1` as an observation-only probability family after extracting and proving a pure Blend 0.4 production boundary. Prospectively stamp six immutable arms — production control, the predeclared 0.65-basis/0.5-slow hypothesis, exact settlement-average mechanics, basis only, production-capped intraday only, and half-strength slow terms — on every new forecast issuance. Each arm selects its own funded-capable side and fee-aware v22 admission result from issuance quotes while inheriting production confidence; unavailable exact inputs stay unavailable. Reuse the append-only forecast journal and independent venue-outcome patches rather than create a duplicate writer/resolver, and require a later immutable manifest before citing V3 evidence. Keep every candidate out of forecast selection, paper/live policy, persistence, portfolio, execution, sizing, budget, reconciliation, and orders by source invariant. Start at 2026-08-25T03:17:17.456Z with 8/8 complete issuance rows and zero production replay error. Gate progress at 10 independent windows for wiring, 100 closed windows with 95% funded-outcome coverage, and 300 closed windows with 90% per-arm availability; counts permit only the next observation phase and never promotion. Production Blend 0.4, buy policy v22, capital, and execution remain unchanged. See `docs/forecast-model-and-evaluator-v3-design.md`. |
| 2026-08-24 | Approve `bounded-taker-pilot-v1` as a funded but hard-bounded production execution experiment, not a buy-policy candidate or promotion. Under execution generation `maker-high30-requalify3-fresh1c-bounded-taker-pilot-v7`, deterministically assign 25% of eligible first-episode, sub-30pp baseline-maker intents to one fresh-quote taker IOC and retain 75% on incumbent maker control. Keep the shared v22 entry rule, existing high-edge route, reduced sizing, exits, and every account guard; additionally cap treatment at 30c per authorization, 300c/10 authorizations total, 80 assignments, two authorizations/hour, one/settlement timestamp, 14 days, and 150c gross realized treatment losses. Treatment reruns the production venue buy rule at the refreshed ask and retains the one-cent movement and active price/spread ceilings, but intentionally waives the 30pp/10pp-median/2c execution-style gates so the pilot answers a question the existing route cannot. Assignment is identical across live/paper; fills and capital remain separate. V1 has no promotion state and can authorize only a later fresh efficacy design. See `docs/bounded-taker-experiment-design.md` and `reports/live-opportunity-review-2026-08-24.md`. |
| 2026-08-24 | Start `exit-policy-sentinel-v2` as a new prospective evidence generation without changing production `strict-value-v1` or selecting new arms after outcomes. Preserve v1 files as immutable diagnostics. Enrol every newly filled edge position independently of first-quote availability; stamp each track's immutable `entryDecision.executionPolicyVersion`; classify every successful evaluator cycle as observed or unavailable before settlement; and use at least 90% observed-cycle coverage instead of a fixed wall-clock-gap proxy. Score paper candidates through the same displayed-depth reduce-only IOC model as production paper exits, including no-fill, partial-fill remainder settlement, and taker fees; missing trigger-time depth makes a paper path incomplete. Keep live replay explicitly optimistic. A review flag requires 60 independent windows, 20 divergent windows, positive cash and clustered mean, one-sided Holm family-wise significance across the frozen four-arm family, and simultaneous positive live/paper eligibility. Collection stays detached and public-data-only; promotion remains a separate manual policy/version and capital/downside decision. See `docs/positive-edge-execution-exit-sentinel-design.md` and `reports/prospective-exit-sentinel-v1-review-and-v2-repair-2026-08-24.md`. |
| 2026-08-24 | Separate local disk reclamation into rebuildable-cache cleanup, verified off-machine restore, and owner-aware immutable tiering. S3-compatible object storage never replaces POSIX hot state: canonical ledgers, control, reconciliation checkpoints, indexes, open sets, and active journals stay local; generic pressure handling cannot truncate or delete them. Archive frozen JSON/JSONL derivatives, require stable source capture, and independently restore every manifest object under path, SHA-256, and byte-count validation before deletion. Remote-primary eviction remains disabled until a durable tier catalog is published and the object set has Object Lock/enforceable retention or a second independently verified bucket; the current bucket had suspended versioning and no Object Lock on 2026-08-24. Exact `.next/cache` and `.next/dev` removal is permitted as rebuildable housekeeping while production output remains untouched. Require an operating reserve of at least 10% of total filesystem capacity using blocks available to the worker; the check fails below that threshold but cannot infer permission to delete durable evidence. No forecast, policy, execution, capital, accounting, reconciliation, or evidence-retention semantic changes. See `docs/object-storage-retention-and-disk-safety-design.md`. |
