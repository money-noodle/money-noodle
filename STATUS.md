# Money Noodle — Implementation Status

> **Living status document · Updated 2026-08-14**
> Product requirements and architectural decisions are maintained in [`SPEC.md`](SPEC.md).

## Executive summary

Money Noodle is operational locally as a research dashboard, continuous paper shadow trader, and explicitly armed live Kalshi trader. Core side-aware UP/YES and DOWN/NO order submission, reconciliation, failure recovery, quiescent pause, portfolio constraints, contract provenance, independent venue targets, and observation-model plumbing are implemented. The first 100-window evaluator ran without changing production and found insufficient, negative-return trade evidence. Venue reference comparability is now measured and remains approximate by rule. Major remaining work is hardening evaluator/promotion criteria, learning reduce-only HOLD/EXIT/SWITCH/BUY choices from position paths, measuring maker queue/adverse selection, and proving profitable edge without contaminating the tradeable probability with venue prices.

| Area | Status |
|---|---|
| Dashboard and live feeds | Functional |
| Forecast tracking and performance | Functional; evidence still accumulating |
| Signed venue accounts | Kalshi verified live; Polymarket read/account integration only |
| Paper execution | Continuous and independently funded |
| Live execution | Mechanically functional and active; lifetime realized P&L **+$5.70** across 372 settled entries in 211 windows at a 21.0% win rate, with high tail concentration and 56% of attempts unfilled; no real switch yet |
| Model calibration | 210 independent windows; runs through 200 retained production baseline and changed nothing |
| Production durability | Local atomic JSON; startup/periodic/manual reconciliation, failure injection, guarded auto-resume, and quiescent pause implemented |

## Current measured state

Follow-up review snapshot: **2026-08-11 05:47 UTC**. A 52-minute read-only monitor collected 208 samples across the 05:00, 05:15, 05:30, and 05:45 boundaries without changing configuration or operator intent.

- Forecast record: **19,325 resolved qualifying updates**, **1,106 resolved asset-cycles**, and **210 independent settlement windows**; 74.4% update accuracy, 70.5% cycle-balanced accuracy, and 0.193 Brier.
- The monitor resolved three traded windows plus one no-trade window. All five live fills lost: BNB −8.32¢, XRP −8.32¢, SOL −8.40¢, DOGE −9¢, and BNB −8.04¢, for **−42.08¢**. Five resolved live no-fills included four DOWN outcomes and one missed BTC UP; conservative paper settled six trades for −21¢.
- Since the prior 00:02 review, live added 33 settlements across 19 windows, 7 wins / 26 losses, and lost **51.33¢** on 265.33¢ stake. Lifetime live is now **187 settled across 109 windows, 48 wins / 139 losses, +5.90¢, +0.4% raw ROI**. Window-clustered mean return is +1.6% ±16.5pp—effectively indistinguishable from zero.
- Since the 15:23 Resume: 88 settled first-attempt fills across 48 windows, 23 wins / 65 losses, +151.27¢ on 702.73¢ exact stake (+21.5% raw ROI). Window-clustered mean fell to +14.8% ±29.8pp.
- Tail concentration is now stronger: the same 5.8¢ DOGE and 10¢ BTC wins total +195.61¢, or 129.3% of post-Resume profit. **Excluding those two leaves −44.34¢.**
- Post-Resume asset results now include DOGE +165.11¢, BTC +77.63¢, BNB +49.83¢, HYPE +18.56¢, XRP −20.21¢, ETH −29.98¢, and SOL **0/14, −109.67¢**. SOL remains the clearest adverse segment, but no filter was changed.
- Lifetime maker evidence weakened during the added sample: attempt 1 mean filled return fell from +9.9% to **+4.9% over 173 resolved fills**; all attempts are now −0.6%. Paired maker return gap is −4.2pp ±25.0pp and paired win-rate gap −8.1pp ±5.4pp over 71 windows. Attempt 2 remains disabled at 1/12 and −79.2%.
- Walk-forward run 200 strengthened the case for retaining—not changing—the production baseline: baseline mean window return +4.08% across 100 held-out windows/72 trades versus candidate +1.59%/68 trades. Candidate beat baseline in only 2/5 folds and parameter mode was only 2/5; production remained unchanged.
- Operational execution remained healthy: 99 successful control/dashboard monitor reads showed automation active, reconciliation ready or briefly running, no real blockers, no periodic failures, no ambiguity, and exact local/venue position agreement. Ten observed periodic completions all passed. Final state had zero positions/reservations, 184¢ available, and +84¢ conservative current-period P&L.
- Five monitor reads exceeded the monitor's 12-second HTTP deadline. Follow-up dashboard probes returned HTTP 200 in 3.75–11.48 seconds and control probes in 0.10–1.89 seconds; server logs showed no corresponding API/trading error. This is dashboard/feed latency, not an execution or reconciliation failure, but should remain observable.
- The temporary accepted-order `not_found` issue from the prior review did not recur during this monitor. Current drawdown reporting still uses starting rather than peak equity; exact post-Resume drawdown is currently 68.27¢ below peak and maximum observed remains 73.26¢.
- Superseded by the 2026-08-14 mirror/audit deployments: paper now simulates the managed maker path, and new orders preserve issuance, approved, submitted/amended, and authoritative-fill prices separately instead of overwriting `askPrice`.

The follow-up does **not** support calling the recent profitability stable. It supports retaining the model weights, stake, and retry controls. Binary buy policy v13 restores the selected-side probability floor to 55% for both live and paper after the prospectively monitored v12 near-floor fills lost; the venue-independent probability model remains unchanged.

Binary-entry deployment verification at **06:36 UTC**: live was paused/drained to restart-safe before the change; all then-current tests, TypeScript, and production build passed; startup reconciliation found zero positions/reservations/discrepancies; automation was explicitly resumed under the unchanged 10¢ purchase cap. The first post-deployment cycle produced one clean UP no-fill and two full UP maker fills (DOGE 8.06¢ exact stake and ETH 7.80¢), with reconciliation ready and no API errors. Later organic BNB and DOGE DOWN maker fills established signed NO exposure; manual authoritative reconciliation confirmed two local/venue managed positions and 18¢ reservation with zero failures.

Standalone-exit deployment exposed shared-account interference before restart: external/manual SOL NO orders on the same ticker netted against a Money Noodle YES fill, so Pause reconciliation correctly blocked on venue quantity 0 versus local 0.47. No state was overridden. After authoritative settlement cleared the contract, a second Pause/drain passed restart-safe, the 180-test exit build started with clean reconciliation, and automation was explicitly resumed. Do not manually trade the same active crypto ticker while automation owns or may enter it.

## Implemented

### Forecast and research

- Next.js App Router dashboard with responsive cards, charts, countdowns, data-health states, factor drill-downs, and a consistent Ramen Gold (`#E3B04B`) Money Noodle theme across controls, charts, focus states, glow, selection, and app icon.
- Polymarket, Kalshi, Kraken, CoinGecko, CoinDesk, and historical ingestion for configured crypto assets.
- Venue-independent contract-basis forecast using one Kraken series for cycle reference, current price, and realized volatility.
- Binary buy policy v13: at least 55% independent selected-side probability, at least 5pp expected edge after fees, at least 50% estimate quality, and an enabled actionable selected-side UP/YES or DOWN/NO entry from 5¢ through 97¢. The 52.5–55% v12 experiment remains separately identifiable in durable issuance evidence and returns to observation-only status.
- Venue prices are benchmarks and execution costs, not inputs to the tradeable probability.
- Every qualifying 15-second calculation and a bounded one-minute sample of non-qualifying calculations are tracked; accuracy, Brier/log loss, calibration, cycle-balanced metrics, benchmarks, and realized-versus-predicted edge are reported.
- Signal-quality metrics are explicitly separated from live and paper trade records.
- Multi-provider server-side LLM research with isolated Pi authentication and browser-local multi-turn history. LLMs cannot configure or execute trades.

### Accounts and execution

- Signed production Kalshi balances, positions, orders, fills, and v2 order submission.
- Polymarket public/CLOB account monitoring; no Polymarket live placement.
- Explicit total live budget verified against signed Kalshi cash.
- Fixed all-in per-purchase amount including fees, with a separate environment stake ceiling.
- Kalshi fractional sizing in 0.01-contract increments.
- Managed post-only GTC maker entries for both sides with passive repricing, tapered tick handling, cancellation, and authoritative fill/fee reconciliation. YES buys map to Kalshi bids; NO buys map to signed YES-book asks while ledger costs remain NO-denominated.
- Exact sub-cent principal, fee, stake, and realized P&L reporting with conservative whole-cent local reservations.
- Continuous paper shadow execution with a separate bankroll and ledger.
- Explicit `TRADE LIVE` arming, environment opt-in, kill switch, pause/resume, maximum stake, maximum filled venue orders per hour, and automatic pause on ambiguous failures. User Pause is a quiescent drain protocol: withdraw operator intent immediately, wait behind the serialized execution queue, cancel/confirm managed remainders, reconcile authoritatively, and report `restart safe` only with zero working or uncertain transactions. Filled positions may remain durably open. Production verification paused with two filled positions, completed manual reconciliation with zero pending/uncertain intents, reported `quiescent · restart safe`, and then resumed without changing the 15¢ authoritative reservation.
- Hourly order limit counts unique Kalshi entry/exit orders with an actual nonzero fill; unfilled and rejected attempts do not count.
- Execution-only signal maturity gate shared by paper and live: no entry in the first 90 seconds, current qualification plus 3 persistent snapshots spanning at least 30 seconds, median net edge ≥5pp, and no new entries in the final 120 seconds. Persistence is keyed by asset/window/side, so UP evidence can never authorize DOWN or vice versa.
- Current positive-edge cards join against the exact live asset/contract order and distinguish collecting, ready/not-attempted, working, filled/open, attempted/no-fill, rejected, switched, and settled states. Automation explicitly reports when all current signals were already attempted instead of incorrectly claiming no edge exists.
- The Automation summary includes a collapsed-by-default expandable Open orders panel, separated into live and paper tracks, showing side, venue, lifecycle, quantity, exact cost/entry, current owned-side probability, close time, executable return, contract ID, and profit-lock state. New orders also persist and expose expandable `entry-decision-v1` evidence: policy, selected-side probability, quality, actionable quote, fee, spread, net edge, persistence, basis, settlement observation, factor details, and execution decision.
- A separately fetched paginated Decision history at `/api/trading/history` combines durable edge-buy evidence with grouped attempts, fill terms, current/terminal status, outcome, and P&L for live and paper tracks without enlarging the dashboard polling payload. Legacy orders show reconstructed core values and identify missing pre-snapshot detail.
- Side-aware standalone reduce-only exits and live position switching. Standalone policy uses one fresh snapshot: strict value exits when executable cash beats optimistic hold by 1¢, plus a +75%-armed profit reversal when both executable value and independent probability decline from high water. Switching supports a full portfolio plus protected same-asset opposite-side reversal even before the cap:
  - compares net liquidation value plus replacement expected profit against incumbent hold value;
  - includes exit spread/fee, replacement principal/fee, and realized exit loss;
  - requires replacement-side probability to exceed the owned side by 15pp, or 20pp for same-asset UP↔DOWN reversal;
  - uses side-aware reduce-only IOC exits and can never create reverse exposure through SELL;
  - withholds the replacement after zero or partial exit;
  - blocks switches inside the final 120 seconds and after one completed switch per window.

### Durability and verification

- Atomic JSON cache, forecast history, trading control, provider settings, and execution ledger writes.
- Unique temporary paths and damaged-history prefix recovery/quarantine.
- Serialized local background collection and execution.
- Hard startup reconciliation barrier reads complete paginated Kalshi orders/fills/positions/resting orders and cash before live execution; managed resting remainders are canceled and confirmed, durable client/venue IDs recover lost responses, contradictory state pauses, reservations are rebuilt without artificial P&L, and results are exposed in UI/audit history.
- Full reconciliation also runs automatically every 5 minutes by default (server-configurable, clamped to 60–3600 seconds). It is serialized behind execution so it cannot race a managed order. A first periodic failure soft-blocks new live orders and retries after 30 seconds; a second consecutive failure safety-suspends and audits. Unchanged successful periodic checks do not bloat the audit ledger; status exposes trigger, next run, and failure count.
- Operator intent is separate from operational state. Explicit Pause/kill, reconfiguration, mode changes, depletion, and all legacy paused records set paused intent and can never auto-resume. Ambiguous-order/switch and reconciliation failures create a system-originated safety suspension only when automation was already explicitly active. Successful authoritative reconciliation then auto-resumes only if the retained active intent remains and every normal funding, connector, environment, budget, and reconciliation readiness check passes. A user can cancel pending auto-resume while suspended.
- Ambiguous entry and reduce-only errors retain reservations with `uncertain` state, safety-suspend, and automatically trigger authoritative reconciliation after the engine transaction finishes. Kalshi is retried through a 30-second consistency window before an absent client ID can be called rejected. A resolved system suspension may guarded-auto-resume only with retained active intent and every readiness check clear; manual/kill/configuration pauses remain paused.
- Deterministic failure-injection coverage includes accepted/lost responses, lost and unconfirmed cancellation, partial maker fills, complete/partial reduce-only exits, signed DOWN entry and DOWN reduce-only recovery, restart recovery and positive/negative position contradictions, Kalshi amendment chains plus aggregate duplicate-overfill detection, stale/insufficient venue cash, malformed collections, and unrelated resting orders.
- Durable aligned quarter-hour cycle paths backfill and append one oracle point per 15-second bucket. Observation-only diagnostics include sign-flip rate, lag-one return autocorrelation, trend efficiency, range, quadratic cycle-local volatility, and an explicit regime label; issuance-time prefixes are persisted with forecasts and exposed in the Cycle regimes UI/segments without entering probability, confidence, ranking, or gates.
- Historical multi-cycle streak analysis (`reports/cycle-streak-analysis-2026-08-11.md`) retained 1,006 complete asset-cycles across 143 windows. Unconditional next-DOWN was 50.93%; after at least two prior DOWN cycles it was 47.93% (95% clustered interval 39.38–56.48%). Magnitude, path smoothness, assets, and chronological halves were unstable. Only 14 exact same-Kalshi-contract fixed-snapshot return rows were available and were negative after costs. No production change is justified.
- Warm-up timing analysis (`reports/warmup-entry-timing-analysis-2026-08-11.md`) covered 220 settled live fills across 125 windows. Performance was not monotonically better later, but the 34 fills entered 60–90 seconds after open lost 75.52¢ at −28.5% capital ROI and showed a −26.8pp model-versus-win gap; both chronological halves were negative. A descriptive 90-second threshold retained +207.93¢ versus +132.41¢ at 60 seconds. The approved bounded safety adjustment is deployed: production blocks the first 90 seconds while retaining the existing persistence gate.
- End-cycle live fills are concerning but sparse: seconds 660–780 produced 0/8 wins and −59.62¢ across six windows, while all 10 accepted maker intents with outcomes lost. Broader fixed-snapshot replay across 178 windows found 2–5-minute mean return +1.51% with uncertainty spanning zero, so the final-120-second production cutoff remains unchanged. Track granular late cohorts and a final-240-second shadow candidate rather than selecting a cutoff around one 5.8¢ tail winner.
- Six-hour live review (`reports/live-run-review-2026-08-11-1622.md`) found a severe forecast/regime failure: 5/59 settled hold wins across 23 windows, −334.57¢ exact P&L, and an owned-side probability gap of −28.0pp. Pure taker counterfactual lost 823¢ and underperformed actual maker by 491.05¢; the latest nine resolved adaptive recommendations went 0/9 and lost an incremental 56.18¢ versus maker. Keep maker-only; neither taker nor adaptive qualifies.
- Forecast-failure replay found directional accuracy remained 73.17%, but policy v10 bought 347 model-underdog sides in the six-hour fixed-snapshot cohort; they won only 17.77% and returned −5.24%. A symmetric 55% owned-side floor improved the full 228-window replay from 33.27% to 54.11% clustered wins and from +2.95% to +4.68% fee-aware mean return, with positive chronological halves and both UP/DOWN eligibility. Policy v11 implements the floor without adding venue prices to the forecast.
- Exact-contract Kalshi-weight replay (`reports/kalshi-forecast-weight-analysis-2026-08-11.md`) covered 308 asset-windows across 44 settlement windows and regenerated policy-v11 Kalshi orders from each candidate probability. Kalshi weighting improved Brier score (0.1602 independent versus approximately 0.145 at 0.30–0.50 log-odds weight), but every positive-weight strategy lost after the same Kalshi ask/fee. Expanding walk-forward candidates lost 1.13 normalized units versus baseline +0.33, were positive in 0/5 folds, and beat baseline in 0/5. Tradeable Kalshi weight remains zero. A ≤15pp disagreement veto returned +0.55 on only four trades and remains observation-only.
- Time-of-day review (`reports/time-of-day-analysis-2026-08-11.md`) found one consistent warning: 08:00–12:00 PDT (15:00–19:00 UTC) produced −27.9% live and −38.3% paper capital ROI while fixed five-minute forecast Brier worsened to 0.1753. No positive band was stable across live, paper, days, and clustered returns. With only 2–3 days and major policy/time confounding, no production clock gate is justified; retain the weak band as observation-only and rerun on policy-v11 data after at least 10–20 calendar days.
- Follow-up calendar review (`reports/time-of-day-analysis-2026-08-14.md`) still finds 08:00–12:00 PDT the only cross-track warning: live/paper window-balanced returns are −24.3%/−31.0%, but live uncertainty spans zero and only five dates exist. Adjacent two-hour results reverse sharply, positive bands depend on tail payouts, and policy v17 has only one date in one band. Weekday history has one occurrence per represented weekday and no inference value. The old forecast store is not sufficient for a long study because it prunes nonqualifying rows to 20,000 while retaining qualifiers, creating selection bias in older fixed snapshots.
- Added an observation-only missed-good-buy monitor to Performance. It records current-policy Kalshi sides that pass quality, 5pp net edge, and price gates but fail only the active selected-side floor; every asset is reduced to its five-minute snapshot and every result is resolved against the exact issuance-time Kalshi contract/fee. It reports raw and strongest-per-correlated-window counterfactuals separately, never creates paper/live orders, and explicitly treats later wins as hindsight until held-out clustered evidence exists.
- Manual operator promotion deployed `buy-binary-edge-net5-quality50-owned52.5-price5to97-v12` to live and paper. Prospective review then found 14 near-floor live intents, seven fills, one hold win, −83.38¢ actual P&L after exits, and −115.98¢ hold P&L. Policy v13 restores 55% for live and paper while preserving v12 immutable evidence and monitoring rejected near-floor candidates. Do not relax below 55% without stronger unseen clustered evidence.
- Run review `reports/live-run-review-2026-08-12.md` found +469.74¢ live P&L across 37 exposures/32 windows, but a wide clustered interval and strong dependence on strict exits. It recommends parallel observation-only evaluation of direction-specific regime evidence, quality ≥65%, and a five-minute elapsed-time candidate before any further production promotion.
- Deployed an adaptive current-policy regime gate instead of a fixed clock or raw win/loss streak. A separate exact-Kalshi-ticker sentinel records at most one highest-edge recommendation per correlated settlement window regardless of paper bankroll or gate state, measures bounded fee-aware edge, learns exponentially weighted mean/variance, warms for 12 policy-v11 windows, soft-blocks only new exposure at 99% negative-return confidence, and automatically reopens below 75%. It preserves active operator intent and all exit/reconciliation processing; manual pauses and hard drawdown/lifetime stops retain non-auto-resume semantics. Durable state/audit lives in `data/regime-gate.json` and both automation surfaces expose its evidence. Follow-up runtime inspection corrected two non-trading issues: approximate cross-venue rule comparability no longer suppresses same-ticker sentinel collection, and a cached prior-window Kalshi quote is discarded after a quarter-hour boundary instead of appearing live until discovery catches up.
- Exit reconstruction found 16 full live exits realized +235.04¢ versus +426.27¢ from holding: profit reversal was −130.36¢ incremental in 9 cases and strict value was −60.88¢ in 7. No exit threshold changes are promoted from this small hindsight cohort, but standalone exits now receive authoritative hold outcomes/P&L and separate performance reporting so tail-payoff surrender is measurable. An observation-only principal-recovery shadow compares full exit with selling only enough at the realized exit price to recover cost while retaining residual payout.
- Observation-only final-minute settlement-average model uses the Brownian integral variance `T − 2W/3` before the window and conditions on the observed log-price integral inside it. It is benchmarked separately against outcomes and does not replace production `P(UP)` yet.
- Observation-only maker model records Kalshi bid/ask paths and estimates ask-touch probability using a quote first-passage model over the 12-second managed-order horizon. The execution funnel separates post-only race, venue acceptance, queue fill, and settlement; touch is explicitly not assumed to equal fill. Exact queue size ahead and depth priority are not yet observed.
- Bounded maker recovery replaces one-shot suppression: each passive entry is managed for 12 seconds with gradual non-crossing reprices under the original edge-approved cap; an accepted zero-fill or definitive post-only acknowledgement race may receive one new durable retry after 30 seconds only if signal persistence, Kalshi-specific edge, constrained portfolio selection, fresh quote, budget, rate, and final-120-second checks all pass again. Maximum 2 attempts per asset/contract; no stale maker-to-taker fallback. Repeated create races back off one additional exact tapered tick per refreshed submission, and a rejected post-only amend leaves the accepted resting order under management rather than being treated as ambiguous.
- Adaptive maker/taker capability is implemented but production remains `MONEY_NOODLE_ENTRY_EXECUTION_MODE=maker`. Every new live intent records a strict shadow decision using current/median net edge, quality, spread, and comparable empirical maker fill capture. The performance UI separately reports shadow taker recommendations/counterfactual return and any future actual IOC takers. `adaptive` can submit only a price-capped marketable IOC limit after all strict and ordinary gates; no uncapped market primitive exists. First production shadow cycle evaluated three maker intents: HYPE and SOL recommended taker but executed maker, while XRP retained maker; actual taker orders remained zero.
- Live ledger presentation groups durable attempts by logical asset/window intent while retaining every raw attempt for reconciliation and fill-model reporting. It labels `post-only race` separately from `rested · no fill` and marks a later successful attempt as `recovered on retry`.
- Constrained portfolio selection ranks candidates by expected dollar contribution after principal/fees, then applies same-window and same-group exposure ceilings plus explicit correlation penalties. A second side of the same asset/window is blocked outside the validated reduce-only switch path. Default live/paper policy allows at most 2 positions per settlement window and 1 per correlation group with a server-side configurable global cap defaulting to 3 and hard-limited to 10.
- UI/engine persist separate `qualified`, `portfolio-selected`, `switch-candidate`, and `blocked` states with exact reasons and portfolio ranks. A Polymarket-only edge can no longer authorize a live Kalshi order whose own quote fails policy.
- Switch hardening requires liquidation-cost-adjusted gain plus a configurable uncertainty margin, a default 15pp replacement probability advantage (20pp for same-asset opposite-side reversal), 3 distinct snapshots spanning 30 seconds, minimum-gain hysteresis across the streak, and a configurable 180-second completed-switch cooldown. Completed switches record independent hold outcomes and combined switch-path P&L for switch-versus-hold reporting.
- Persistence spans are measured by distinct 15-second observation buckets rather than raw scheduler milliseconds, preventing valid three-snapshot evidence from becoming stuck at 29/30 seconds while still rejecting duplicate timestamps.
- Product branding, package metadata, runtime labels, user agents, browser storage, internal runtime symbols, client IDs, documentation, and server configuration now use **Money Noodle** / `MONEY_NOODLE_*`. The supplied Money Noodle artwork now has one universal transparent PNG/WebP lockup and social card using Ramen Gold for MONEY/tagline and brand green for NOODLE/rules, plus 16/32/64/192/512 icons, favicon, Apple icon, and Next metadata assets. The same lockup remains readable on light and dark screens, the header uses the mark, and dense 7–11px evidence labels receive a global one-pixel readability increase without changing component spacing. Reconciliation still recognizes pre-rename exit IDs, and browser research chat migrates from its legacy key.
- Forecast storage remains exact but no longer reparses and atomically rewrites the full history every 15 seconds. A process cache loads the 200MB legacy snapshot once; new records, resolution patches, and retention tombstones append to `forecast-history.journal.jsonl`, with idempotent replay and 50MB snapshot compaction. Browser and collector requests share fresh dashboard calculations, historical aggregation is cached for one minute, chart history is bounded to 500 representative points, and performance history rows use a compact projection.
- **392 unit tests across 64 files**, passing TypeScript, and passing production build. The authoritative-provider and authenticated-dashboard build was deployed through pause/drain/reconcile/restart; temporary local `AUTH_PASSWORD`/`AUTH_SECRET` values are configured, reconciliation is ready, and live automation was explicitly resumed with zero open orders/reservations.
- Unauthenticated visitors on the persistent worker see the complete paper-trading side of the system and nothing else. `GET /api/paper-budget` serves the read-only Budget dialog: simulated starting/available/equity/reserved funds, current paper cap, aggregate outcome counts, P&L, reset count, and the newest 30 grouped sanitized paper execution intents. A public paper automation panel reports whether the simulated shadow is active, idle, or bankroll-depleted, its realized P&L, shadow bankroll, open/settled/outcome counts, and its open simulated positions. The positive-edge buy panel is public because the calculation is derived on the client from public predictions and fixed policy thresholds; its per-candidate live-execution and portfolio-readiness badges are omitted because `GET /api/trading/control` stays signed-session-only. `GET /api/paper-performance` serves the complete public positive-edge track record and backs a paper-only mode of the full performance dialog: whole forecast scoring (accuracy, cycle-balanced accuracy, Brier/log loss, streaks, calibration bins and lock status, benchmarks, edge buckets, segments, lead-time slices, timeline, missed-buy counterfactual), the 500-row signal history, cycle regimes, walk-forward evaluations, and the complete paper `TradeTrackRecord` including segment breakdowns and switch/exit counterfactuals. It omits the signed payload’s `liveRecord`, live-only maker-fill report, private target-integrity registry report, and worker-local prospective policy/calendar evidence; those tabs are removed in public mode. Forecast records are compacted to history rows, so factor weights, contract provenance, and calibration-replay snapshots never leave. The public payload still excludes all live figures/records, accounts, provider permissions, client/venue identifiers, decision snapshots, and mutations; control, live-budget, account, provider, live performance, LLM research, and LLM-provider-management surfaces remain signed-session-only. The public Policy link exposes immutable model/buy/execution/exit/switch/regime details and history, but never the private provider registry or permissions.
- Vercel is explicitly stateless: startup reconciliation, collector timers, execution, and every durable write are disabled; dashboard caches are warm-instance memory only. It returns public market research with a server-side redacted payload and labels its paper ledger unavailable rather than fabricating serverless performance. Signed private routes return a clear persistent-worker-required `503` instead of touching absent files. Production deployment: `https://noodle.money`.
- Added an optional managed-Postgres paper projection foundation: `db/migrations/001_public_paper_projection.sql`, lazy read-only hosted projection reads, and best-effort worker-only replication guarded by `MONEY_NOODLE_POSTGRES_PAPER_SYNC=true`. JSON remains authoritative. Both migrations are applied to the attached Neon database and `MONEY_NOODLE_POSTGRES_PAPER_SYNC=true` is set on the worker, so the hosted dashboard now serves real replicated paper figures.
- **The maker shadow was inverted with it (2026-08-14).** `buildMakerShadow` assumed every order filled at the ask, so once paper began resting at the bid its two columns silently swapped meaning: the realized maker return was being reported as the ask benchmark, and then multiplied by a fill probability the order had already survived. It is now execution-aware — `askReturn` always means "entered at the ask" and `expectedMakerReturn` always means "rested at the bid, fill risk included", whichever way the order actually executed — so the series stays readable across tonight's boundary instead of flipping halfway through. For maker-executed orders the ask side is re-priced from the recorded bid plus spread and no fill probability is applied, because the order already survived that risk. **Remaining caveat, stated rather than hidden: for the maker era this population is conditioned on having filled**, since an unfilled attempt never settles and has no payout to re-price, so the always-fills column is optimistic there until unfilled attempts are joined to their forecast outcomes — the same join `buildMakerFillReport` already does.

- **Paper now executes as a maker, like live (2026-08-14).** Paper filled instantly at the ask, which is not perfect execution — it is *taker* execution, charging the mirror a spread and a fee that live's resting orders never pay, on every trade, while missing none of the trades live misses. Paper now posts the same resting order live posts: re-priced to the bid via `restAtBid`, held for the same `MANAGED_MAKER_HORIZON_SECONDS` window, filled by `resolveRestingPaperOrders` only when a recorded ask actually reaches the limit, and marked `unfilled` with the reserved stake returned when it does not. Qualification is unchanged and still uses the actionable ask, because posting passively does not entitle the desk to assume a better entry — only the simulated execution moves. **Expect paper volume to fall by roughly the live no-fill rate (46%) and paper entry prices to improve by about the spread**, so paper P&L before and after this change is not comparable; new orders carry `liquidityRole: 'maker'` and a `restingUntil`, which distinguishes them. The always-fills benchmark is not lost: `buildMakerShadow` already scores the same signals at the ask, and it becomes the ask-fill counterfactual rather than the maker one. Evidence that motivated it: paper's own maker shadow scored 618 modelled orders at **−8.2% at the ask against +2.8% when filled as a maker** (clustered −6.0% vs +3.0%).

- **Paper retry leakage fixed; the requested pause now starts after cancellation (2026-08-14).** The first maker-paper deployment only blocked an open/resting order. Once an attempt expired, the next 15-second collector tick could submit the same logical paper order again under the same ID, without the live attempt cap or cooldown; five windows accumulated 14 records in the first half hour. New paper attempts now use the exact same bounded-attempt decision and maximum as live, with durable `:retry:2` identity when a second attempt is explicitly enabled. A terminal maker attempt records `makerCompletedAt`, and the 30-second retry cooldown is measured from that cancellation/completion time rather than from submission (which had silently reduced the pause by the 12-second resting horizon). After the pause, the existing engine still rebuilds the order from the fresh dashboard and re-runs persistence, policy, venue edge, quote, spread, portfolio, funding, rate, and final-120-second checks before any retry. Production remains one attempt (`MONEY_NOODLE_MAX_LIVE_MAKER_ATTEMPTS=1`); this fixes mirror accuracy and the dormant retry path without promoting second attempts.

- **The maker fill model was inverted, and is now empirical (2026-08-14).** `quote-first-passage-v1` estimated the probability of the ask reaching our resting bid within a 12-second horizon. Validated against 623 recorded live attempts it was **worse than uninformative**: sorted into buckets by its own prediction, observed fill rates ran **66%, 61%, 57%, 52% against predictions of 12%, 41%, 64%, 86%** — monotonic and backwards. Mean predicted fill was 69.9% on orders that filled and 73.6% on orders that did not. The mechanism is queue position, which a touch model cannot see: a tight spread makes a touch look easy and simultaneously means a deep queue we are standing behind. Replaced by `maker-fill-empirical-v2`, which shrinks the observed fill rate of the same price/spread cohort toward a 55% base rate with a 20-attempt prior, computed at order-build time where the ledger is available. **Walk-forward on the same 623 attempts, each scored using only prior orders: Brier 0.3425 → 0.2461.** Being blunt about what that buys: always predicting the base rate scores 0.2474, so the fix removes an actively harmful signal and adds almost none. Price and spread bands carry essentially no information about fills; discriminating between attempts would need book depth the desk does not collect. The first-passage estimate is retained as a recorded diagnostic so it can be re-validated rather than silently dropped.

- **Where the live fill rate actually goes, and why not-filling is currently helping (2026-08-14).** 778 submitted maker attempts → 688 accepted (90 rejected as post-only races) → 370 filled, 318 rested and expired: **53.8% filled given acceptance**. Every order posts at the bid (`quoteDistance` equals the full spread) with a 12-second managed horizon, so a fill requires the ask to fall to our price — the market must move against the side being bought. The selection that produces is **favourable on the measure that matters**: filled attempts returned **+4.1%** while accepted-no-fill attempts would have returned **−5.0%** at their own posted price, against win rates of 28.6% and 31.8%. Fills win less often and pay more, because they are bought cheaper. **Blanket taker execution is therefore worse than it looks:** the −5.0% counterfactual is priced at our resting bid, and crossing means paying the ask (mean spread 1.56¢ on a ~31¢ contract, ≈5% of stake) plus a taker fee of ≈1.5¢ (≈4.8%), turning a −5% cohort into roughly −15%. Recorded fees on 372 settled live entries total **$0.01**, confirming maker fills are effectively free while the entry gate subtracts a fee anyway — so the desk currently demands ~6.5pp of true edge to clear a 5pp gate on a 30¢ contract. The gated case remains open: the adaptive shadow has recommended taker on 40 of 332 evaluations, all resolved, at **+31.7% mean return**, but that cohort was selected using the fill model just found to be inverted and must be re-run before it can be cited.

- **Paper became a true mirror of live; buy policy v17 (2026-08-14).** The tracks had diverged in three places at once: paper traded XRP that live withheld, paper ignored the adaptive regime gate live obeyed, and DOWN was controlled by two separate per-track flags. That made `paper − live` unreadable — a disagreement between the books could be a policy difference, a fill difference, or a cohort live never traded, with no way to tell which. The entry rule layer now takes **no execution-mode parameter at all**, so a divergence cannot be expressed: `qualifiesAsBuyEdge`, `hasTradableEdge`, `bestEntry`, `bestEntryForSide`, `bestVenueEntry`, `qualifiesVenueBuyEdge`, `downEntryEnabled` and `assetAdmitted` all lost their `mode` argument, and `lib/mirror-invariant.test.ts` asserts that by arity so it cannot creep back. `MONEY_NOODLE_SUSPEND_DOWN_ENTRY_LIVE`/`_PAPER` collapse to **`MONEY_NOODLE_ALLOW_DOWN_ENTRY`, currently enabled**; `MONEY_NOODLE_LIVE_EXCLUDED_ASSETS`/`PAPER_EXCLUDED_ASSETS` collapse to **`MONEY_NOODLE_EXCLUDED_ASSETS=XRP`**. The version bump is deliberate even though no threshold moved: paper records written before the alignment came from different rules than live's and must stay distinguishable from mirror records. **Consequence, accepted rather than worked around: paper stops trading XRP**, so XRP evidence pauses until the evaluation lane of SPEC §12 restores it as a candidate — it already clears −2se on both tracks, so more of the same is worth less than a mirror that can be trusted. Portfolio selection needed no merge: both tracks already call the same `selectPortfolio` with the same constraints, differing only in which book's exposures they count, which is correct.

- **DOWN/NO suspension withdrawn; buy policy v16 (2026-08-14).** The v14 suspension was reversed because its evidence does not reproduce. It cited DOWN at **−58.0% ±14.7 over 44 settlement windows**, accounting for **−$17.86 of a −$18.62 lifetime live result**. Re-scored from the order ledger using the convention the desk's own `buildTradeRecord` uses — settled entries, stake as `actualStakeCents ?? stakeCents`, P&L as `payoutCents − stake` — **live DOWN is −8.9% ±16.8 over 56 windows (noise), and lifetime live P&L is +$5.70, not negative.** Excluding XRP, which is withheld separately, **paper DOWN is −0.4% ±10.7 over 80 windows**. Four P&L conventions (payout−stake, the `pnlCents` field, each with and without exit records) all return +$5.70; eight cumulative cutoffs show live DOWN never worse than −17.1% ±20.8 at any point in its history; a double-counted-fee hypothesis that matched the $24 gap closely is refuted by recorded fees of $0.01 total on settled live entries. The order population matches the original analysis (334 settled at 23.1% win rate then, 372 at 21.0% now) — the same book, different arithmetic. **This is not evidence DOWN is profitable:** the interval spans roughly −43% to +26% and its point estimate is still negative. It is a statement that no measurement supports removing ~55% of the desk's volume, which is what DOWN was of pre-suspension entries. DOWN trades again on both tracks and both keep scoring it. The control is inverted rather than deleted: a switch suspends the side again without a code change. *(Superseded hours later by v17, which collapsed the per-track pair into one `MONEY_NOODLE_ALLOW_DOWN_ENTRY` covering both tracks.)* **The XRP exclusion is unaffected and was re-confirmed in the same pass** — live −45.7% ±21.5 over 41 windows and paper −35.1% ±13.0 over 81, each clearing −2se independently, reproducing the original figures. The DOWN loss is not the XRP loss: XRP is mostly UP trades (30 of 41 live), and removing it moves live DOWN only from −8.9% to −8.6%.

- **Open: the 35pp ceiling refuses the best-performing traded cohort (2026-08-14).** Scored on claimed edge recorded at entry, the 35pp-and-above bucket returns **+63.5% ±49.1 over 10 windows, +$20.56** (live +$6.00, paper +$14.56) against 5–10pp at +14.3%, 10–20pp at −7.8%, and 20–35pp at +0.4%. It does not clear +2se and only 196 of 1,099 settled orders carry a recorded edge, so this is weak — but it is the second independent look pointing the same way, after the six live trades at +$2.19 already recorded above. It stands against forecast-level calibration over 218 windows, which is a different question (calibration, not realized P&L) on a much larger sample. The ceiling costs no volume — it blocked zero windows across 16:00Z–23:14Z — so it is retained pending more windows, not defended.

- **The published policy had drifted from the code, and now reads it (2026-08-13).** The policy surface described v13 while production ran v15: the manifest's history entry inherited `BUY_POLICY_VERSION` but kept v13's activation date, summary, and changes, so neither the DOWN suspension nor the edge ceiling appeared anywhere a reader could see, and the one test asserting history integrity was satisfied by the version bump itself. History entries are now literal strings with v14 and v15 recorded from their commit timestamps, so **bumping the constant fails the test until the change is described**. Every tunable control is read from the same function execution reads — `regimeGateSettings()`, `switchPolicySettings()`, `maximumLiveMakerAttempts()`, `maximumNetEdge()`, `downEntryEnabled()`, `excludedAssets()` — rather than hardcoded, and a new eligibility component publishes the per-track DOWN and asset withholdings that previously had no surface at all. Two dashboard claims were simply false and were corrected against their constants: a 60-second execution warm-up that has been 90 seconds since the first implementation, and a "15% prediction-market factor weight" that is 0.30 with a basis term and 0.80 without.

- **The production forecast model has no promotion record (2026-08-13).** The promotion ledger is empty — `data/model-promotions.json` does not exist — so Blend 0.4 runs without a recorded justification, and the newly published provenance surface says so in those words rather than rendering an absence. `unrecorded` is computed as "the newest ledger entry does not name the version production is running", which also catches the subtler case of a ledger whose latest entry promotes something else. The record is withheld from the public payload alongside `tradingProviders` and `performance`, since its reasons and held-out evidence are derived from the private track record. Promotion remains manual: writing the entry requires an operator's reason, and none was invented.

- **Edge ceiling at 35pp, buy policy v15 (2026-08-13).** A claimed edge of 35pp or more is now treated as model failure rather than opportunity: `MAX_NET_EDGE = 0.35`, applied at both the shared admissibility check and the inline `qualifiesVenueBuyEdge` path, env-tunable via `MONEY_NOODLE_MAX_NET_EDGE` and restrictive only. The evidence is forecast-level and well-powered — across 37,943 resolved forecasts, the 35pp-and-above bucket predicts 64.0% and realizes 37.6%, a **−26.4pp gap holding at −26.7pp clustered over 218 windows**, while every bucket below it is calibrated or conservative. **A tension worth recording: of the 46 settled live entries that recorded an edge, the six at or above 35pp returned +$2.19 while the forty below returned −$14.47.** Six trades carry no information and cannot outweigh 218 windows, but the only live sample at this edge level points the other way, and that should be revisited once more live entries record an edge. Test fixtures that relied on implausible edges were brought inside the ceiling rather than the ceiling being weakened to accommodate them.
- **Execution and full position paths now collect prospectively (2026-08-14).** New orders preserve issuance bid/ask/spread, approved maximum, first submitted limit, every create/accept/reprice/reject/cancel/fill event, and authoritative average fill separately; normal execution and startup recovery no longer destroy issuance evidence by replacing `askPrice`. Optional 20-level Kalshi YES/NO ladders record displayed size at the selected limit and better, best-level depth/imbalance, quote touches, resting duration, fill shape, and cancellation-confirmation latency. This is explicitly a queue-ahead proxy, not private priority; depth failure cannot block trading. Every fresh cycle also appends executable bid/ask, exit fee, net liquidation, exact cost, unrealized P&L/return, independent probability/quality, basis/regime/clock, and depth to every open paper/live position, including positions retained after a failed standalone exit. Signed order details expose raw paths and Maker execution reports aggregate coverage. No price, retry, sizing, selection, exit, or gate changed. See `reports/execution-observation-instrumentation-2026-08-14.md`.
- **Venue target comparability is now explicit and measurable (2026-08-14).** Immutable provenance parses and fingerprints opening-reference and closing-settlement windows, simple-average/TWAP method, and decimal rounding. Current contracts align at 60 seconds but differ on Chainlink TWAP versus CF Benchmarks simple average, so the reasoned label remains `approximate`; known close/window mismatch fails to `not-comparable`, and `exact` requires oracle/method/window identity. Signed Performance → Target integrity joins retained cycle paths without rewriting history: 1,100 Kalshi reference joins show −0.0210% mean signed / 0.0268% mean absolute Kraken drift (0.1943% maximum), while bounded final-minute Kraken proxies agree with 92.8% of 839 exact Polymarket outcomes and 93.6% of 800 exact Kalshi outcomes. Twenty-eight paired asset-windows disagree across venues within 218 settlement timestamps. Proxy paths never substitute exact venue results and no probability, gate, order, budget, sizing, or promotion behavior changed. See `reports/contract-comparability-analysis-2026-08-14.md`.
- **Calendar-effects lane now collecting prospectively (2026-08-14).** `calendar-effects-v1` commits one non-pruned fixed-five-minute observation for every exact Kalshi asset/window whether or not it qualifies, carrying model/policy versions, probability, confidence, both books/fees, selected side/edge, compact factor values, cycle regime, and exact settlement. It separately retains one first actionable highest-edge current-policy candidate or an explicit no-candidate marker per correlated window, with fee-aware ask outcome and the decision-time empirical fill-weighted maker benchmark. Evidence uses an append-only crash-replayable journal with 50 MB snapshot compaction, retains superseded policy cohorts without blending them, and appears only in signed Performance → Calendar. Review is fail-closed: every four-hour Pacific band needs 30 dates and 100 resolved candidates; every weekday needs 12 occurrences and 100 candidates. Sample readiness cannot alter gates, sizing, budget, orders, paper/live behavior, or promotion.
- **Two-snapshot persistence candidate now collecting prospectively (2026-08-14).** A current-policy retrospective screen over the broad v10–v13 binary history made two consecutive observations worth testing but not promoting: it roughly doubled signal-level candidates (64 → 130), with +9.4% ±9.8pp clustered fee-aware ask return across 71 windows versus +12.1% ±12.4pp for production's three observations across 43; the 66 added candidates were +12.1% ±14.1pp over 44 windows, far too uncertain and unstable by side (UP positive, DOWN negative). Allowing interrupted observations was weaker: two qualifying observations within 45/60 seconds expanded to 144/151 windows while clustered return fell to +0.9%/−0.9%, so the candidate remains **two consecutive**, not nonconsecutive. `persistence-two-consecutive-v1` now records forward intents in a separate worker-local ledger with exact Kalshi ask/bid/fee terms, whether production was already ready, production catch-up delay, the then-current empirical maker-fill estimate, and exact settlement. It ignores capital and current positions to isolate signal maturity, is scoped to the active buy-policy version, appears read-only in the signed Performance view, and has no order, reservation, paper, live, or promotion path. Production remains three snapshots over 30 seconds. The first manual review is locked until 100 resolved incremental settlement windows; reaching the count changes nothing automatically.
- **Where the losses come from: narrowed, with three hypotheses refuted (2026-08-13).** Testing rather than reasoning eliminated three explanations in turn. **(1) The edge filter does not destroy calibration.** Across 37,943 resolved forecasts bucketed by predicted edge, calibration is sound or conservative everywhere below 35pp, and on the band the policy trades (edge ≥5pp, probability ≥55%) forecasts realize **75.8% against 67.3% predicted — 8.5pp better than claimed**. Only the extreme bucket breaks: **edge ≥35pp predicts 64.0% and realizes 37.6%, a −26.4pp gap holding at −26.7pp clustered over 218 windows**, which is well-powered and actionable. **(2) Maker fill selection is not the cause.** Paired on the same windows, filled attempts against accepted-but-never-filled counterfactuals give a win-rate gap of −4.6% ±4.3 and a return gap of +2.9% ±16.1 over 123 windows — neither clears two standard errors, and filled attempts returned *more* (+4.1% against −5.1%). **(3) The gap is not an artifact of mixed policy generations.** Splitting by era makes it worse, not better: superseded versions −17.3pp ±3.1 over 152 windows, current era **−33.1pp ±6.5 over 42 windows**. A check that the 55% floor was violated also failed — the policy-recorded probability matches the derived one on all 46 orders carrying both, and the sub-floor entries are all from versions that predate the floor. **What remains:** qualifying forecasts beat their own prediction by 8.5pp while executed orders miss theirs by 33pp, with fill selection excluded. The untested difference is everything else between qualification and execution — the 60-second wait, three persistent snapshots over 30 seconds, the final-120-second cutoff, portfolio and correlation caps, and re-entry cooldown. Signal persistence is the largest of these and the natural next test.
- **XRP withheld from live entry on its own evidence (2026-08-13).** Clustered by settlement window, XRP is the only asset whose per-window return clears two standard errors negative, and it does so independently on both tracks: **live −44.3% ±21.4 over 41 windows, paper −34.3% ±13.1 over 80**. Nothing else qualifies on either track — SOL −27.9% ±17.7, HYPE −14.6% ±23.6, BTC +38.3% ±35.7, and BNB +22.6% ±20.6 are all indistinguishable from noise. **The maker shadow's apparent BTC +27.2% and BNB +5.6% advantage did not survive this test and was a ranking artifact**; excluding or favouring an asset on a ranking alone is how a five-day sample becomes a permanent rule. Seven assets were tested, so roughly 0.4 false positives are expected by chance, which is why agreement across two independent samples rather than magnitude is what justified acting. Live-only by default at the time, so paper kept trading XRP while real money stopped. *(Superseded 2026-08-14 by v17: one `MONEY_NOODLE_EXCLUDED_ASSETS` list now covers both tracks, so paper withholds XRP too and the measurement moves to the evaluation lane.)*
- **Maker-shadow cohorts, tail concentration, and per-asset segments (2026-08-13), closing item 2.** Chronological cohorts show the shadow improving: **earlier half −13.0% ask / −2.6% maker-weighted, recent half +2.8% / +9.6%** across 140 windows each. Tail concentration is reported as absolute contribution, and share only when the total is positive — a share of a negative total inverts its sign and reads as the opposite of its meaning, which the first implementation did report before it was corrected. Across 280 windows totalling **−14.26**, the best window contributed **+5.05** and the best three **+13.90**; removing those three leaves **−28.16**, roughly doubling the loss. The book is materially carried by three windows. Per asset, maker-weighted: **BTC +27.2%** (94 windows) and **BNB +5.6%** (107) positive; **HYPE +1.2%**, **DOGE −0.9%**, **SOL −7.2%**, **ETH −9.1%**, **XRP −19.9%** (64) negative. XRP and SOL were also the worst live cohorts, so the two measurements agree.
- **Immutable manual model promotion and hardened promotion thresholds (2026-08-13), item 3.** The 225-window checkpoint referenced by the old roadmap item passed 223 windows ago; the evaluator has run **14 walk-forward evaluations** and is now at **448 windows** with the next checkpoint at 450. It has always refused to change production on its own, but nothing recorded what *is* in production or on what evidence. `data/model-promotions.json` is now an append-only ledger: a promotion or rollback requires a written reason, captures the cited walk-forward run and its evidence so the two cannot drift apart, and a rollback appends a new entry naming what it supersedes rather than deleting anything — the evidence behind a past decision is exactly what a later review needs. `evaluatePromotionEligibility` is deliberately stricter than the evaluator's own `candidate_passed_review_thresholds`, which marks a candidate worth reviewing rather than worth deploying: promotion additionally requires at least 60 held-out candidate trades, 4 of 5 folds positive, 4 of 5 folds beating the baseline, a 2pp mean-window-return gap, and near-exact replay. Evaluated against the newest run the answer is **not eligible** — 115 held-out trades and exact replay pass, but the evaluator retained the baseline, only 3 of 5 folds are positive, 3 of 5 beat the baseline, and the candidate is **0.65pp worse** than the baseline rather than 2pp better. The ledger is empty, which is now a recorded fact rather than an unknown: nothing has ever been promoted.
- **Maker-execution paper shadow (2026-08-13), item 2.** Paper enters at the ask and always fills — conservative on price, unrealistically certain on execution — while live rests a post-only limit at the bid, where roughly half of attempts never trade. That divergence was invisible in paper's own numbers. `buildMakerShadow` reprices each settled paper entry at its recorded bid, holding quantity and outcome fixed so only entry price and fee change, then weights by the recorded `quote-first-passage-v1` fill estimate; an unfilled order earns nothing rather than losing. Across **724 settled paper entries (614 with a fill estimate, mean modelled fill probability 72.0%)**: immediate-ask **−7.9%**, maker when filled **+3.2%**, maker weighted by fill chance **+3.4%**. Clustered over **280 settlement windows**: immediate-ask **−5.1%** against maker-weighted **+3.5%**. So paper's execution model, not its forecast, accounts for roughly 9 percentage points per window, and paper has been understating what the same signals would return under live-style execution. Observation only: it changes no gate and places no order. Exposed as `paperMakerShadow` on the signed performance payload.
- **Explicit stake-expansion criteria (2026-08-13), completing item 8.** `evaluateStakeExpansion` states whether the per-trade cap may be raised and never raises it: expansion stays a manual, audited act, in the same spirit as model promotion. Five criteria, all of which must hold — at least 30 settled settlement windows in the **current epoch**, a clustered mean window return that is positive and clears **two standard errors**, drawdown from peak within 10%, lifetime realized P&L across every epoch not negative, and a budget that is neither depleted nor unconfigured. Returns are clustered by settlement window because correlated assets in one window are a single bet, and the two-standard-error bar is the same test already applied to segments and maker cohorts; against the roughly 98-percentage-point per-window spread on this book it is hard to clear by luck. Thresholds are env-tunable and the proposed cap is reported even when ineligible, so the target stays visible. Evaluated against current state: **not eligible** — 211 windows and lifetime +$5.70 pass, but the clustered return is **+8.6% ±10.7** (does not clear two standard errors) and drawdown from peak is **23.8%** against a 10% limit. This is the gate item 13 depends on, and it now says plainly that the book has not earned a larger stake.
- **Durable budget epochs (2026-08-13).** `configureTradingBudget` rebased funded capital and zeroed `realizedPnlCents` with no epoch identity, so earlier trades became unattributable — which is why reconstructed history disagreed with the control record. The control now carries `epochId`, `epochSequence`, and `epochStartedAt`, minted on every reconfiguration, and each order is stamped with `budgetEpochId` at creation so a later reconfiguration cannot reattribute its P&L. Peak equity resets with the epoch, since carrying a previous peak would measure drawdown against capital the new budget never held. Orders written before epochs belong to a named `legacy-pre-epoch` rather than being folded into the current one, which would credit past results to a budget that never funded them. `GET /api/performance` now reports `liveEpochs` per epoch plus `liveLifetimePnlCents`, so current-epoch P&L may restart while lifetime results stay immutable. **This quantified a real gap: the control reports −$4.77 for the current epoch while the order ledger holds +$5.70 across all live history, $10.47 that no surface previously attributed to anything.** All 781 live orders currently sit in `legacy-pre-epoch`; the first reconfiguration mints `epoch-1`.
- **Live drawdown is now measured from peak equity, not the funded amount (2026-08-13).** `evaluateLiveRisk` compared current equity against `startingBudgetCents`, so a run-up masked the fall that followed it: fund $20, reach $32, drop to $22, and the reported drawdown was **zero** while the real fall from peak was $10 — the 25% stop could not fire after a gain, which is when the most is at risk. `BudgetControl.peakEquityCents` now records the epoch high, updated on every settlement, and drawdown is measured against `max(peak, starting)`. The reference never falls below the funded amount, so the change can only tighten the stop and never loosen it, which is asserted directly. Reconstructing from settled live orders, equity peaked near **$32.25** against $20 funded and fell **$10.42** from that peak — more than double the $5.00 stop — without the old measure ever showing it. Records written before peak tracking fall back to the starting budget and reproduce previous behaviour exactly.
- **Unclassified-path windows refused entry on both tracks (2026-08-13).** Windows whose aligned 15-second path the classifier labels `insufficient` no longer produce entries, on live and paper alike. In-sample counterfactual over 321 settled live orders joined to their recorded `cycleRegime`: trading only classified windows turns **−$14.98 into +$1.13** (ROI −22.4% → +11.3%), and the excluded cohort carried **−$16.11 at −28.4% ROI** across 194 trades. With DOWN already suspended, UP-only goes from −$0.99 to **+$1.54** (+16.0% ROI). **Clustered by settlement window neither cohort clears two standard errors** — kept +11.1% ±18.7 over 87 windows, removed −20.3% ±14.0 over 130 — and the split was chosen after inspecting the data, so this is a restrictive bet on a plausible mechanism rather than a validated filter. It was accepted because it can only remove trades and never add exposure: the downside is missed opportunity, not new risk. Both tracks now record `entryCycleRegime` on every order, so future cohort analysis needs no rejoin against the 210MB forecast snapshot, and `MONEY_NOODLE_REQUIRE_CLASSIFIED_REGIME=false` disables it. The live filter is applied to the candidate list rather than the chosen order, so an unclassified best candidate steps aside for the next rather than skipping the cycle.
- **Regime features do not explain the UP/DOWN gap (2026-08-13).** Joining 321 of 335 settled live orders to their recorded `cycleRegime`, the gap persists inside every bucket, and **all 69 DOWN entries fall in `insufficient`** — DOWN is only ever taken in windows the classifier could not characterise, which is why the trend features have nothing to say about it. UP spans all labels and wins 28–33% in classified windows against 22% in `insufficient`. Entry timing is near-identical by side (UP median 636s remaining, DOWN 628s), but win rate decays far faster for DOWN as the window progresses: early 17%, mid 5%, late 0 of 9, against UP's 29/23/21%.
- **The tie-break hypothesis is dead (2026-08-13).** Settlement resolves `≥` toward UP, so near-flat windows were a candidate explanation for the DOWN deficit. Across 1,200 recorded cycles there were **2 exact ties (0.17%)**, 11 within 0.001%, and a median absolute move of 0.085%. Ties are too rare to matter and cannot account for a 44-window, −58% deficit.
- **DOWN/NO entry suspended after a loss review (2026-08-13).** Live lifetime is **−$18.62** across 334 settled orders at a 23.1% win rate, and essentially all of it is one side. Clustered by settlement window, DOWN returned **−58.0% ±14.7 over 44 windows** (clears −2se) versus UP at **+2.9% ±13.1 over 162 windows** (noise), and DOWN accounts for **−$17.86** of the total. The split is temporal as well: DOWN was 0% of first-half entries with +$0.36 P&L, and 41% of second-half entries with −$15.10. Regime does not explain it — 60.9% of traded asset-windows resolved DOWN, a base rate favouring DOWN bets, yet asset-windows where DOWN was selected resolved DOWN only **11.6%** of the time, worse than random in an environment tilted its way. Buy policy is now `…uponly-v14`; DOWN is gated per track behind `MONEY_NOODLE_ALLOW_DOWN_ENTRY_LIVE` / `MONEY_NOODLE_ALLOW_DOWN_ENTRY_PAPER=true`, applied at both the shared admissibility check and the inline `qualifiesVenueBuyEdge` path so no entry route is left uncovered. Exits, reduce-only sells, and settlement of existing DOWN positions are unaffected. The observed six-loss streak that prompted the review is **not** itself evidence: at a 23.1% win rate a run of six has a 20.8% chance from any starting point, there have been 20 such runs already, and the longest is 25.
- **Calibration is materially off, with enough evidence to act on (2026-08-13).** Across **194 independent settlement windows** the model predicted 47.0% average selected-side probability and realized **26.3%**, a **−20.8pp** gap, consistent across buckets: 54.4%→25.8%, 63.5%→32.1%, 72.9%→50.0%. The 55% selected-side floor is therefore admitting trades whose realized frequency is closer to 26%. Predicted edge is additionally **anti-predictive**: 0–10pp returned −8.7%, 10–20pp −56.4%, 20–30pp −81.7%, 45pp+ −100%, and contracts under 15¢ won 7% of 58 attempts. Large claimed edge concentrates at cheap prices where the model disagrees most with the venue, and the venue is right.
- **Why DOWN differs — first evidence (2026-08-13, small samples).** Outcomes were compared against the market-implied probability in the ask. UP roughly tracks the venue price (0–25¢ ask won 15% against 13% implied; 25–45¢ won 31% against 35%), while DOWN falls far below it (0–25¢ won **3%** against 13% implied; 25–45¢ won 21% against 35%). Sorting by model-versus-market disagreement, outcomes degrade monotonically on both sides: UP won 67% at 0–15pp disagreement, 29% at 15–30pp, 18% above 30pp; DOWN won 22% at 15–30pp and **0%** above 30pp. The venue price appears to be the better probability estimate, and the model's deviation from it is at best uninformative — which challenges the premise that edge comes from disagreeing with venue-implied volatility. Disagreement-bucket samples are 7–11 orders and must not be treated as settled; the price-bucket comparison behind them spans 34–128 orders. Worth testing next against the observation-only `cycleRegime` features already recorded per forecast (sign-flip rate, lag-one autocorrelation, trend efficiency), since a mean-reverting 15-minute window would punish any bet on the direction of an existing deviation, and the settlement rule resolves ties toward UP, which is an asymmetry a symmetric normal CDF around the basis does not model.
- **Crypto.com and Robinhood account access verified live (2026-08-13).** Both signed schemes authenticated on first attempt, validating constructions that had only been unit-tested: HMAC-SHA256 over Crypto.com's sorted key-value concatenation (`method + id + apiKey + params + nonce`), and Ed25519 over Robinhood's `apiKey + timestamp + path + method + body`. Through the app's own `/api/accounts`: kalshi $16.28 with 1 position, crypto-com $65.28 available of $73.69 cash with 3 positions (BTC 0.0008, DOGE 250, SHIB 1,130,000), robinhood $0.10 with 2 positions (SOL 0.129, ETH 0.005), polymarket unconfigured. A half-configured provider fails closed: with a Crypto.com key present but no secret, the connector reported unconfigured and made no request rather than attempting to sign. Both connectors remain read-only by construction — neither module contains an order, cancellation, or mutation function.
- **The two providers are not interchangeable as data sources (2026-08-13).** Crypto.com's public reads need **no credentials** and expose 930 instruments (577 spot pairs, 343 perpetual swaps) with real order-book depth and a **$0.01** spread on BTC near $63.7k. Robinhood has **no order book**: `best_bid_ask` returns spread-inclusive quotes with ~0.945% each way, about **1.9% round trip**, which no measurable edge survives. Crypto.com is the research surface and needs no account to use; Robinhood is an account source only. Registry readiness strings for both `crypto-spot` pairs now state this verbatim, so the limitation travels with the capability flag.
- **Kalshi signing key rotated after a git exposure (2026-08-13).** `Trading App.txt` in the repo root was the live Kalshi RSA signing key referenced by `KALSHI_PRIVATE_KEY_PATH`, committed in the first implementation commit and present in every tree since. It was untracked, ignored, and purged from all 29 commits with `git filter-repo`, then force-pushed. **The rewrite did not un-publish it:** the blob remained fetchable at commit `5395b37` afterwards, because GitHub retains unreachable objects, so the key was treated as exposed and rotated. The repo is private with zero forks, which bounded the exposure. The new key lives at `~/.config/money-noodle/kalshi-private-key.pem` (mode 600, outside the tree) with a new key ID; a signed `portfolio/balance` read returned HTTP 200. Loose copies in `~/Downloads` and a stale `.env.local` backup were securely removed, and `.gitignore` now covers `*.pem`, `*.key`, `*.p8`, `id_rsa*`, and `.env*.bak*` after a timestamped env backup nearly repeated the mistake during cleanup. Outstanding: confirm the old key ID `437c…` is revoked at Kalshi, and ask GitHub Support to purge unreachable objects.
- **Failed sign-ins are throttled (2026-08-13).** The publicly reachable login route had no rate limiting, lockout, or delay, so a single password stood between the internet and a control plane that arms real-money trading, and guessing ran at network speed. `lib/login-rate-limit.ts` adds a fixed one-second delay per failure plus a five-failure fifteen-minute per-IP lockout, with a bounded client map that evicts expired entries first, forgiveness for failures spread beyond the window, independent client buckets so an attacker cannot lock the operator out, and the same delay applied to locked-out requests so probing lockout state costs what guessing costs. A malformed body now returns 400 instead of throwing a 500 and is not counted as an attempt. Verified locally: 400 on JSON, five failures at about a second each, sixth 429 with `retry-after: 898`, success path still issues a session cookie. **Verified on production the lockout does not fire** — seven attempts all returned 303 because Vercel spread them across warm instances and no process saw the burst. The delay held on every attempt. Treat the delay as the guarantee and the lockout as best-effort; the stronger fix is removing the public sign-in surface entirely, since nothing on `noodle.money` needs a session.
- **Repository security audit (2026-08-13).** All history scanned for private keys, provider tokens, AWS and Slack prefixes, Postgres URLs, and `AUTH_*`; the Kalshi key was the only real secret ever committed, and every other hit is a placeholder in `.env.example`, `README.md`, or a test fixture. `data/` is untracked apart from `.gitkeep`, so ledgers, order identifiers, budgets, and forecast history never entered git. No secret logging, no accidental data dumps, `spawn` uses a fixed binary with array arguments and no shell, and the only `dangerouslySetInnerHTML` holds a static uninterpolated string. Auth uses `timingSafeEqual` with an `httpOnly`, `sameSite=lax`, production-`secure` cookie. Open: 21 of 24 dependencies are pinned to `"latest"` including `next`, `react`, and `typescript`; and `passwordMatches` compares lengths before the constant-time comparison, which leaks the password length.
- **Read-only Crypto.com and Robinhood account connectors (2026-08-13).** `lib/cryptocom-api.ts` reads `private/user-balance` with HMAC-SHA256 over a sorted key-value concatenation where parameter order is part of the signature; `lib/robinhood-api.ts` reads accounts and holdings with Ed25519 over `apiKey + timestamp + path + method + body`, accepting the raw 32-byte seed Robinhood's generator emits. Neither module contains an order, cancellation, or mutation path, since no market grants either provider execution capability. Signing is isolated into pure functions and tested without credentials, including verifying a real signature against a generated public key and rejecting an undersized key rather than padding it. Account types widened to any provider while execution readiness filters explicitly to the two trading venues, so a read-only provider can never surface as a venue the engine could arm. Unconfigured providers fail closed with no network call.
- **Perpetual funding rates carry no usable signal on current evidence (2026-08-13).** See the SPEC decision log: 20 Bitstamp perpetual markets, pooled and clustered correlations disagreeing in sign, no market clearing two standard errors, and a sign-following rule negative after fees. Underpowered rather than settled — 99 independent intervals against a standard requiring 100 — so accumulate funding history before revisiting a `crypto-perp` market.
- **Robinhood event contracts verified not viable (2026-08-13).** The only official interface is the Crypto Trading API at `trading.robinhood.com`: crypto only, US only, API key plus Ed25519 request signing via `x-api-key`/`x-timestamp`/`x-signature`, covering accounts, holdings, orders, products, and quotes with market/limit/stop order types. The official article documents no event-contract, prediction-market, equity, or option endpoints. Robinhood's prediction markets are additionally reported — not officially confirmed — to route to the Kalshi exchange, which Money Noodle already trades directly, so such an adapter would add a broker hop rather than a market. Its crypto API is real but every endpoint including quotes is account-authenticated, so unlike Crypto.com nothing can be exercised without operator credentials.
- **`crypto-spot` added as a research-only market (2026-08-13).** Both Crypto.com and Robinhood expose crypto trading APIs and no event-contract API, so this is the market their interfaces actually serve. Capabilities are market-data only with paper and live false: a continuous-payoff instrument needs a sizing and exit model and, more fundamentally, a directional expectation the zero-drift forecast does not produce, so paper results would measure an unspecified strategy rather than the model. Crypto.com public Exchange v1 instruments, tickers, and order-book reads were confirmed working without credentials. Provider-level capability flags are now gated on the **production** market rather than a union across markets: a union would have reported Crypto.com as research-capable because of `crypto-spot`, and `crypto-15m` surfaces reading the provider-level flag would then have treated it as a research provider for contracts it cannot see. Existing tests caught that fail-open before it shipped.
- **Crypto.com event contracts verified not viable (2026-08-13).** The binary product is Strike Options from Crypto.com | Derivatives North America under CFTC oversight, US only, in 5-minute/20-minute/2-hour/daily/weekly durations with no 15-minute contract. Three independent blockers: the Exchange API v1 trades spot, margin, perpetuals, and futures but not Strike Options or Up/Down, and its Predictions API is data-only with execution restricted; there is no order book, since orders are IOC market orders against a platform-quoted price with a slippage tolerance, which makes managed post-only maker placement impossible and leaves no two-sided book from which to derive implied volatility; and settlement uses CDNA's own per-second BID/ASK-midpoint index against a predetermined strike with a fixed US$10 payout, which is `not comparable` to the 15-minute cycle-open target. Capabilities remain false and the registry continues to fail closed. The supported spot/perpetual instruments belong to a future market gated behind directional-alpha research, since the current model assumes zero drift. Reversing this requires confirmation from CDNA or Crypto.com institutional support that Strike Options is programmatically tradeable.
- **Market identity and per-provider tracking implemented (2026-08-13).** `lib/market-registry.ts` declares `crypto-15m` and per-(provider, market) capability as the single source of truth; the provider registry and config store now derive capability from it instead of restating it, removing two duplicate tables. New orders carry `marketId`, and records written earlier default to `crypto-15m`. Trades, unfilled maker attempts, and rejections split per (provider, market) within each execution mode through `buildProviderTradeRecords`, attributing the 1,394 orders written before `providerId` existed to their venue, which is what that field always held. Verified by independent recount of the live ledger: paper kalshi 695 settled at +$27.86; live kalshi 360 settled, 389 unfilled, 4 rejected at +$6.13; the split reconciles with the combined record. The signed performance payload carries both modes' splits and the Trades tab renders them; the public projection carries the paper split only. No budget or execution behavior changed.
- **Per-provider budgets with market allocations implemented (2026-08-13).** `data/provider-budgets.json` (`provider-budget-v1`) holds a per-provider ceiling plus a percentage allocation per market, enforced as a hard cap on what each (provider, market) pair may commit, computed against current provider equity so caps compound with wins and contract in drawdown. It is a limit layer, not a second cash ledger: cash accounting stays single-source in the legacy control and paper ledger, since two ledgers of the same money drift and the drift appears as a reservation one believes affordable. Splitting cash per provider waits for a second funded live provider, when balances can be reconciled against real venue cash. Seeded on first read from the legacy control without rewriting it, and with **no** provider ceiling: seeding the original funded amount would cap a provider at its starting balance and leave realized gains undeployable. A market is charged only its own reservations, cash still bounds the cap, and the skip reason names whether allocation or cash is binding. Verified against the running worker — seeded from legacy revision 2485, legacy control untouched at $15.74 available, live sizing unchanged at the $2 per-trade cap, and the allocation math probed at 10% → 157¢, 5% → 78¢, 10% with 100¢ committed → 57¢, 0% → skip. `lib/global-exposure-caps.test.ts` fails if position, same-window, or correlation caps ever gain a provider dimension.
- **Remaining from the keying decisions:** per-(provider, market) policy overrides for entry thresholds, sizing, and execution style; and the candidate-set selection order (funding as a feasibility filter, then ranking by expected contribution at fundable size). Both touch the live entry path. The selection-order change has no present effect while Kalshi is the only live-capable provider, so it should land with the second live provider rather than ahead of it.
- **Budget, model, and policy keying decided (2026-08-13); model calibration keying not yet implemented.** Budget per provider with percentage market allocations off current provider equity as hard caps, because cash is not fungible across provider accounts; forecast model and calibration per market so every provider in a market reports the identical probability; entry thresholds, sizing, and execution style per (provider, market); position, same-window, and correlation caps global per market so splitting budgets cannot silently multiply correlated exposure. See the SPEC decision log for the reasoning and the rejected alternatives.
- Extended that projection to the public track record with `db/migrations/002_public_paper_performance.sql`. The scored payload is stored as one JSONB document because the hosted dashboard reads the whole row and serves it verbatim; normalizing calibration bins, segments, timelines, walk-forward runs, and 500 forecast rows would add a schema migration to every scoring change for no reader benefit. Replication runs from the background collector, throttled to once a minute since scoring reads the entire forecast log, is never awaited by a collection or execution path, and refuses to publish a non-durable payload so a hosted read can never be echoed back into the projection. `durable` and `generatedAt` are re-derived from the replication timestamp on read rather than trusted from the stored copy, and a payload missing its scored halves is treated as a failed replication rather than an empty record.

## Implemented design milestones

> These milestones are retained as an implementation and safety reference. They are complete and are not part of the remaining roadmap.

### 1. Durable Kalshi reconciliation — core startup barrier implemented

Implemented on every startup before allowing a new order:

- Venue positions.
- Resting orders.
- Historical and recent fills.
- Client order IDs and venue order IDs.
- Local available and reserved budget.
- Orders previously classified as rejected, unfilled, pending, or uncertain.

A lost HTTP response after Kalshi accepts an order now retains funds and is recovered by durable client ID. Unconfirmed maker cancellation blocks and pauses. During the calibration-replay deployment, a BNB maker was accepted between the pre-restart pause and process shutdown; startup correctly blocked on unconfirmed cancellation, manual authoritative reconciliation confirmed zero fill/no position, and the 10¢ reservation was released before live resume. The matcher handles complete and partial reduce-only recovery without auto-submitting a replacement, but the first real complete or partial switch has still not occurred and that path remains operationally unproven.

### 2. Failure-injection testing — implemented

Deterministic tests now cover:

- Accepted order followed by timeout/lost response.
- Cancel response lost or cancellation state uncertain.
- Partial maker fill.
- Partial reduce-only exit.
- Restart with open positions or resting orders.
- Duplicate client order ID.
- Stale or contradictory account balance.
- Malformed venue responses.

Ambiguous states safety-suspend while retaining reservations until authoritative venue reconciliation completes; they are not converted into rejected/no-position states merely because a request failed. Automatic reconciliation retries through the venue consistency window. If the suspension was system-originated, active operator intent was retained, and every normal readiness check passes, guarded auto-resume is allowed; manual/kill/configuration pauses never auto-resume.

### 3. Time and cycle-regime model — observation layer implemented

Implemented in observation-only mode:

- Record 15-second underlying paths for aligned `:00/:15/:30/:45` cycles.
- Sign-flip rate.
- Lag-one autocorrelation.
- Trend efficiency.
- Cycle range and cycle-local volatility.
- Explicit final-minute settlement-average uncertainty.
- Separate maker-fill/first-passage probability from settlement probability.

These features and both alternative probability models are persisted without changing the live buy gate. They remain observation-only until validated across independent settlement windows.

### 4. Portfolio selection and switching — core constraints implemented

The engine and UI now distinguish:

- **Qualified buy** — clears standalone expected-value policy.
- **Portfolio-selected buy** — belongs in the preferred constrained portfolio.
- **Switch candidate** — beats an incumbent after liquidation loss and all costs.
- **Blocked** — correlation, exposure, insufficient improvement, spread, liquidity, or time constraints.

Implemented: same-window and correlation-group limits, correlation penalties, expected-dollar ranking after costs, switch persistence/hysteresis, cooldown, uncertainty margin/configurable gain, and switch-versus-hold counterfactual tracking.

Maker adverse-selection observation is now implemented. The report separates submission, post-only acknowledgement race, venue acceptance, rested no-fill, partial/full queue fill, and settlement outcome. First-passage calibration is conditional on accepted orders only; post-only races are not mislabeled as queue non-fills. Resolved forecasts provide counterfactual outcomes for accepted no-fill attempts without mutating financial ledger state. The UI compares `P(UP | fill)` with `P(UP | accepted, no fill)`, net realized/counterfactual returns, independent window counts, same-window paired gaps with window-clustered standard errors, and attempt/price/distance/quote-volatility segments. These metrics remain observation-only until enough independent filled and non-filled windows exist.

### Live loss containment and observation fixes

- Live was quiescently paused and authoritatively reconciled before deployment; startup reconciliation restored `restart safe` with no open position or reservation.
- Added configurable current-budget drawdown and immutable lifetime-live loss limits. Both are evaluated from authoritative local accounting; crossing either blocks Resume and guarded auto-resume. Defaults are 25% of configured allocation and 50¢ lifetime loss.
- Deployed one live maker attempt by default, with the hard two-attempt implementation retained only behind explicit server configuration for future validated use.
- Added bounded post-DELETE order polling for Kalshi's observed read-after-delete delay; terminal status with nonzero remainder or expiry still fails closed into reconciliation.
- Corrected live `startingCents` to configured allocation and corrected low/high entry-price buckets in both forecast and executed-trade reports.
- Added risk-policy, retry-cap, cancellation-delay, cross-module reservation/intent, provenance, target-selection, time-alignment, and report-bucket tests. The verified suite is now **191 tests across 35 files**.

## Canonical remaining roadmap

> Only unfinished implementation, evidence collection, and manual review gates belong here. Remove an item when implementation and required production verification are complete.

### 1. Venue-specific target integrity and promotion readiness — **highest priority before 100 windows**

The target-integrity foundation is now deployed:

- `data/contract-provenance.json` is an append-only atomic registry of full venue rules. Forecast rows retain compact issuance-time references containing venue contract ID, close, rules/source fingerprint, reference source/value when published, and comparability state.
- Polymarket condition IDs/rules/Chainlink resolution sources and Kalshi tickers/full rules/floor strikes are captured independently. A rules or reference change creates a new fingerprint rather than mutating history.
- The resolver fetches and retains Polymarket and Kalshi outcomes independently. New returns select the outcome from the same venue/contract as the stored entry; missing provenance and mismatched outcome IDs fail closed.
- Existing rows remain untouched and are labeled `legacy-polymarket` at evaluation/report time. Real venue entries with legacy/mismatched targets are excluded from walk-forward trade-return scoring.
- Kalshi discovery now requires the active market to close on the same quarter-hour as Polymarket. This fixed an observed boundary bug where a still-active prior ETH contract could be paired with the new Polymarket window.
- Cross-module cancellation tests now cover both bounded delayed confirmation with P&L-neutral reservation release and deadline expiry with retained reservation, active intent, system suspension, and authoritative release.

Remaining before the automatic evaluator is decision-grade:

Completed follow-up:

- Prospectively verified venue-specific target identity with genuine cross-venue disagreements and zero fail-closed substitutions.
- Parsed and fingerprinted explicit opening-reference/closing-settlement windows, simple-average versus TWAP method, and decimal rounding. Current Polymarket/Kalshi windows both resolve to 60 seconds, but Chainlink TWAP versus CF Benchmarks simple average keeps the pair `approximate`.
- Added signed Target integrity reporting. Across 1,100 direct Kalshi joins, Kraken reference drift averages −0.0210% signed / 0.0268% absolute with a 0.1943% maximum. Bounded final-minute Kraken path proxies agree with exact outcomes on 92.8% of 839 Polymarket and 93.6% of 800 Kalshi asset-windows; 28 paired asset-windows disagree across venues within 218 settlement timestamps. Exact venue outcomes remain authoritative.
- Added the immutable model promotion ledger and stricter manual eligibility report; automatic evaluation and LLM research still have no promotion capability.

Remaining before the evaluator is decision-grade:

- Harden walk-forward review thresholds so a candidate cannot pass when held-out Brier/log loss, coverage, drawdown, or window-clustered uncertainty regress. Account for candidate-grid selection and report signal-policy return separately from maker-executable return.
- Make confidence/clock inputs replayable before evaluating a replacement for the implicit elapsed-time quality lift.
- ~~Wire model promotion/rollback to an authenticated, quiescent operator route; the immutable registry and eligibility checks already exist.~~ **Done 2026-08-14.** `POST /api/model/promotion` is the ledger's first write path — `recordModelPromotion` previously had no caller, so no entry could be created by any means. It requires an authenticated same-origin session, paused automation, a quiescent restart-safe drain, zero reserved budget, a written reason, and an exactly typed confirmation phrase. A promotion must additionally cite the newest walk-forward run by id and clear every eligibility criterion; a rollback is deliberately not eligibility-gated, because reverting must stay available exactly when the evidence has fallen apart. **Integrity rule:** the route refuses any entry whose model version or parameters differ from what the running code forecasts with, so `unrecorded: false` can never be a lie. Production parameters remain compile-time constants, so promotion stays a deploy-then-record act; the route cannot change the tradeable probability. A promotion form in the Policy view is not built — the ledger is still written by an authenticated client rather than a UI.

The scheduler and replay core remain automatic, but no result may be promoted until these remaining integrity and promotion controls pass.

### 2. Position-lifecycle and queue observations — **next learning implementation after target integrity**

The first-passage model did not explain queue position. Prospective observation-only entry and position paths are now implemented at each collector cycle and every create/amend/cancel/fill check:

Implemented:

- Separate immutable issuance, approved maximum, initial-submission, reprice path, and authoritative-fill fields; old records remain legacy and are not rewritten.
- Paper mirrors the bounded maker horizon and records submission/touch/fill/expiry rather than assuming an ask fill.
- Optional 20-level YES/NO books normalize selected-side best depth, imbalance, displayed quantity at the limit, and quantity at that level or better. It is labeled a proxy, never exact queue position.
- Managed live attempts record create quotes, acknowledgement races, acceptance, management quotes, accepted/rejected amendments, terminal fill shape, resting duration, and cancellation request/confirmation latency.
- Every open paper/live position records executable bid/ask/spread, depth, net liquidation after exit fee, cost, unrealized P&L/return, independent probability/quality, basis/regime/clock, and fixed-horizon liquidation counterfactual.
- Signed order history exposes raw paths; Maker execution reports aggregate path/depth, repricing, cancellation, resting, and position-snapshot coverage.

Remaining evidence work:

- Accumulate enough prospective paths to segment fill and return by displayed-ahead proxy, imbalance, repricing path, resting duration, profit state, probability deterioration, and time remaining, clustered by settlement window.
- Evaluate whether these observations discriminate fills or improve HOLD/EXIT choices; they remain forbidden from execution until held-out evidence passes.

This instrumentation is observation-only. It cannot alter price, retry count, sizing, selection, holding, or live gates until held-out evidence shows an improvement in net return or an explicitly bounded drawdown trade-off.

### 3. Reduce-only sell strategy and unified action ranking

Scope `SELL` narrowly: it may reduce only the UP/YES or DOWN/NO side already owned. It cannot open the opposite side, exceed authoritative held quantity, or turn a partial exit into reverse exposure. New DOWN/NO exposure uses the separate binary BUY path. Evaluate these action candidates together:

- **HOLD** — retain the incumbent to settlement.
- **EXIT TO CASH** — initially attempt to close the full remaining incumbent without replacement; an execution may still fill partially and must then stop and reconcile.
- **SWITCH** — liquidate the incumbent and buy a superior replacement; the protected switch implementation already exists.
- **BUY** — add a new selected position when budget and portfolio capacity permit.

For quantity `q`, compare executable net liquidation value `L = q × owned-side bid cents − exit fee cents` with venue-independent expected hold payout `H = q × 100¢ × P(side)`, where `P(DOWN)=1−P(UP)`. Original cost basis is sunk for this decision, though it remains essential for realized-P&L reporting. With probability uncertainty `u`, use optimistic hold value `H⁺ = q × 100¢ × min(1, P(side) + u)`. A strict wealth-improving exit qualifies only when `L − H⁺` exceeds a configurable minimum-cent margin after spread, fee, partial-fill risk, persistence, and quote freshness. `P(owned side) < 50%`, an unrealized profit, or a recent winning streak alone never qualifies.

Learn and report three reduce-only candidates separately:

1. **Upgrade switch** — existing future-wealth comparison against a better selected buy.
2. **Thesis-break/value exit** — one fresh snapshot sells when current owned-side bid net of fee exceeds uncertainty-adjusted optimistic hold value by at least 1¢.
3. **75% profit-reversal exit** — reaching +75% executable profit arms a lock without selling. Persist executable-value and independent-probability high water; one later fresh snapshot may sell when both decline. Cross-trade winning streaks are excluded.

Switches retain three observations over 30 seconds. Standalone exits intentionally use one fresh snapshot so a fast reversal is actionable. Full sale clears entry persistence and starts a 60-second cooldown; afterward same-asset/window re-entry has no count cap but must rebuild three fresh buy snapshots and clear every normal gate.

Rank recommendations by **risk-adjusted incremental expected cents versus no action**, not raw probability or edge. The portfolio optimizer chooses the desired hold/exit/switch/buy set under budget, global/window/correlation limits; dependent exits execute before replacement buys. UI must show action, rank, executable proceeds/cost, hold value, uncertainty margin, reason, and whether the action is alpha-seeking or risk-limiting.

Implementation persists high-water/arming state and executes full-size reduce-only paper/live exits at the owned-side bid. Zero or partial fill retains the remainder and disables automatic exit retry; ambiguity safety-suspends for reconciliation. Re-entry generations use new durable IDs. Versioned `action-counterfactual-v1` reporting and the first buy-and-hold versus buy-plus-exit comparison are now implemented; they separate the two exit policies, which move in opposite directions, and are reporting-only. Remaining evidence work replaces the approximate HOLD arms with complete position paths once enough have accumulated, and resolves whether `profit-reversal-75-v1` should stay armed. Live results cannot justify stake expansion by themselves.

### 4. Prospective calibration collection and automatic evaluation

The 100-window collection gate has been reached, but evaluation remains review-only and production fitting/promotion stays manual. Calibration progress collapses repeated updates and correlated asset/venue cycles sharing a close timestamp into one window, while retaining asset-cycle diagnostics. Resolved non-qualifying observations count toward window evidence but remain excluded from the positive-edge trade record. Current verified snapshot: **114 independent windows**, 561 resolved asset-cycles, and 9,244 resolved qualifying updates. Review work now includes:

- Evaluate a versioned replacement for the current implicit elapsed-time quality lift so clock effects enter through explicit uncertainty.
- Fit volatility scale.
- Fit venue-independent basis and slow-factor weights. Venue prices remain benchmarks/execution costs and may never become inputs to the tradeable probability.
- Review probability caps.
- Review the 5pp edge and 50% estimate-quality thresholds.
- Use held-out and walk-forward evaluation.
- Correct for winner's curse from selecting the largest apparent edge among correlated assets.
- Evaluate realized return by entry price, time remaining, volatility ratio, venue divergence, asset, and settlement window.
- Promote or remove segments only when realized return remains positive after costs across enough independent windows.

Repeated 15-second updates remain useful diagnostics but do not count as independent calibration evidence.

The automatic evaluator ran at 100 windows and repeats every 25 windows. Its first five expanding chronological folds used one fixed five-minute snapshot per asset/window, re-fitting candidates only on past windows and scoring the next unseen block. Baseline produced 9 held-out trades/1 win and the candidate 8/1; both returns were negative, the decision was `insufficient_test_trades`, and production remained unchanged. Versioned `calibration-replay-v1` issuance snapshots persist raw reference/current price, remaining time, realized volatility, baseline basis probability/weight, capped aggregate slow tilt, individual slow-term log odds, and production cap/probability without venue prices. Production replay is checked at issuance and must reproduce the stored tradeable probability to floating-point tolerance. Historical rows use an explicitly labeled z-score/log-odds reconstruction fallback and each evaluation reports exact versus reconstructed coverage and maximum baseline replay error. Candidate grids now cover probability temperature, venue-independent basis weight, volatility scale, aggregate slow-tilt scale, symmetric probability caps, minimum edge, and minimum quality; select at most the largest apparent edge in each correlated window; score net return after stored fees, Brier, log loss, and drawdown; persist a feature-inclusive dataset fingerprint and full fold history to `data/model-evaluations.json`; and repeat at 25-window checkpoints. Candidate output is review-only and cannot change production. Non-qualifying forecast retention was raised to 20,000 rows so the prospective fixed-horizon dataset survives beyond the 100-window gate.

### 5. Real reduce-only switch verification

The switch engine, reconciliation matcher, partial-exit protection, and switch-versus-hold accounting are implemented and tested, but no real switch has occurred. When an organic candidate occurs, verify authoritative Kalshi orders, fills, fees, remaining position, reservations, replacement withholding/submission, and counterfactual accounting. Never force a switch merely to exercise the path.

### 6. Budget epochs, loss gates, and stake-expansion criteria

The current control ledger intentionally resets realized P&L on budget reconfiguration while performance reporting remains lifetime. Make that boundary auditable rather than relying on UI explanation:

- Assign every budget configuration a durable epoch ID and link reservations, fills, settlements, reconciliation adjustments, and orders to it.
- Report current-epoch, lifetime-live, and paper P&L side by side without blending them; preserve closed epoch summaries through reconfiguration.
- Add configurable realized-loss/drawdown circuit breakers whose evidence cannot be erased by silently reconfiguring a budget. Manual pauses and kill-switch intent remain dominant.
- Define an explicit stake-expansion gate using independent live windows, lifetime net P&L after fees, maker adverse-selection uncertainty, walk-forward results, reconciliation health, and real switch evidence. Passing a model-only evaluator is insufficient.

### 7. Trading-provider variants and policy visibility — foundation implemented

Here “provider” means a prediction-market venue/broker, not an LLM research provider.

- Implemented a typed registry for Polymarket, Kalshi, **Crypto.com**, **ForecastEx**, and **Robinhood**, exposed in every dashboard payload and the separate read-only `/api/trading/providers` route so it cannot be confused with LLM providers. It identifies market-data/paper/live capabilities, readiness, adapter versions, and immutable variant IDs. A new atomic `data/trading-providers.json` store persists normalized configuration and currently mirrors the legacy Budget venue authority; planned providers fail closed under malformed settings. `TradingProviderAdapter` now defines normalized contract, quote, account, order, cancellation, outcome, and explicit capability boundaries. No execution behavior changed. Next, make the store the audited authoritative control plane and add read adapters. New adapters remain read/paper-first and depend on verified official event-contract APIs, operator eligibility, and permitted automation; no scraping path may imply live capability.
- Support immutable provider/model variants covering contract semantics, rule/reference mapping, fees, quote normalization, paper fill assumptions, and execution/reconciliation behavior. Provider prices remain excluded from tradeable probability and confidence.
- Run every configured provider variant continuously in an isolated paper track with exact provider-contract outcomes, separate bankroll/accounting, provider/variant/policy identity, clustered windows, P&L, drawdown, and lifecycle evidence. The control plane and new-order identity fields are implemented; multi-variant paper fan-out remains next.
- Implemented authoritative independent research, paper, and live permissions per provider. A one-time migration promotes `data/trading-providers.json` to `provider-registry-v1`; later Budget venue fields are compatibility projections and cannot overwrite it. Mutations require authentication, same-origin, paused quiescent/restart-safe execution, supported capability, exact variant, immutable audit, and operator-typed live confirmation. Paper and live entry gates consume their respective permissions separately; disabling live does not stop paper or reduce-only position lifecycle handling. New providers still default live-disabled and cannot be enabled before an adapter capability is implemented and promoted.
- Add dashboard, performance, open-order, and decision-history filters for `live`/`paper`, provider, provider variant, and policy version. Never blend live and paper results or hide provider-specific denominators.
- Implemented an always-visible active v13 policy badge and read-only Policy view showing active forecast/buy/execution/exit/switch/regime/provider-variant versions, exact details, activation time, evidence links, and immutable v11–v13 buy-policy history. Remaining work moves this static manifest into the durable model/policy registry and adds complete historical parameter diffs, dataset fingerprints, and audited promotion/rollback lineage.

### 8. Secondary expansion — only after safety and evidence

- Depth-aware sizing and slippage only after the observation-only queue layer validates them.
- Operator fill/order/settlement alerts; reconciliation and drain status UI are already implemented.
- Historical execution replay/backtesting that clearly separates reconstructed execution assumptions from the prospective walk-forward dataset.
- Demo/sandbox venue testing where supported.
- Complete the normalized trading-provider registry and variant schema above before adding provider-specific execution.
- Live Polymarket execution after independent signing, collateral, allowance, fee, and reconciliation verification.
- Consolidated venue exposure/P&L without blending paper and live.
- MongoDB repositories, migrations, indexes, and retention.
- Durable workers, queues, and leases.
- Authentication/authorization for non-local deployment.
- Enforce same-origin/CSRF protection on every mutation or billable research route, not only trading control.
- Deployment hardening, observability, versioned backups, restore tests for critical JSON/audit/evaluation data, secret management, and operational runbooks.

Product improvements such as filters, granular degraded states, richer order/switch history, and stronger news/macro data remain useful but are lower priority than the safety and model-evidence sequence above.

## Remaining implementation and evidence order

### Scheduled review — 2026-08-12

Review the adaptive maker/taker shadow after another day of observations. Report shadow evaluations, taker recommendations, resolved recommendations, independent settlement windows, ask/depth coverage, fee-aware counterfactual return, incremental return versus actual maker/no-fill, clustered uncertainty, concentration, drawdown, and side/price/asset stability. Keep `MONEY_NOODLE_ENTRY_EXECUTION_MODE=maker` unless the documented promotion gates pass; do not infer readiness from recommendation count or fill rate alone.

1. ~~Harden accepted-order `not_found` consistency handling and separate approved limit, issuance quote/spread, amendment path, and authoritative fill fields in storage/UI.~~ **Done 2026-08-14.**
2. ~~Add a separate maker-execution paper shadow with rolling cohorts, tail concentration, and per-asset segments.~~ **Done 2026-08-13.**
3. ~~Harden evaluator thresholds and add immutable manual model promotion/rollback.~~ **Done 2026-08-13.** The 225-window checkpoint had already passed; promotion eligibility currently evaluates as not eligible and the ledger is empty. The authenticated, quiescent write route landed 2026-08-14 — see the roadmap item above.
4. ~~Add explicit averaging-window parsing and Kraken-to-venue reference-drift/comparability reporting.~~ **Done 2026-08-14.** Both venues publish 60-second opening/closing windows, but different oracle/method definitions keep comparability approximate; signed reporting measures reference drift and bounded path-proxy agreement without substituting outcomes.
5. ~~Capture complete position-lifecycle, counterfactual liquidation, and queue/depth observations.~~ **Done 2026-08-14; prospective evidence is now accumulating.**
6. ~~Add versioned HOLD/EXIT/SWITCH/BUY counterfactual reports around the implemented standalone exit ledger behavior.~~ **Done 2026-08-14.** `action-counterfactual-v1` reports each action against the alternative it rejected, split by the policy that chose it, window-clustered, and normalized by stake. EXIT and SWITCH arms are authoritative; HOLD arms are labeled approximate until complete position paths accumulate. BUY remains covered by the maker fill/no-fill counterfactual rather than duplicated. The blended `standaloneExitVsHoldCents` figure was replaced because it averaged two policies that point in opposite directions.
7. ~~Run a separate buy-and-hold versus buy-plus-exit report for strict-value and 75%-armed reversal exits.~~ **Done 2026-08-14.** See `reports/exit-counterfactual-analysis-2026-08-14.md`. **The two exit policies diverge.** `strict-value-v1` beat holding by +30.3% ±14.3pp per window across 75 paper windows (111 exits) and +5.1% ±19.7pp across 27 live windows (30 exits). `profit-reversal-75-v1` lost to holding in both: −192.9% ±81.5pp across 8 live windows (9 exits, t=−2.37, −130.4¢) and −10.1% ±29.3pp across 23 paper windows. The reverse arm agrees — 28 live positions that armed the +75% lock and were never sold gave back −50.1% ±40.6pp against their recorded high water. Holding beat exit-at-last-observation by +1.9% ±9.2pp live and +8.9% ±2.7pp paper, so the HOLD decisions themselves are not the problem. **No behavior changed**: `profit-reversal-75-v1` remains live and armed pending a manual decision under item 9. Live evidence is 9 exits over 8 windows and cannot carry a suspension on its own; the paper arm is negative but not significant.
8. ~~Durable budget epochs, peak-equity drawdown accounting, and explicit stake-expansion criteria.~~ **Done 2026-08-13.** Stake expansion currently evaluates as not eligible; see the entries above.
9. Review model, maker, retry, and exit-policy evidence manually. Promote nothing unless unseen-window net return, calibration, uncertainty, stability, drawdown, and maker execution evidence satisfy separate gates.
10. Capture and authoritatively verify the first organic standalone exit and reduce-only switch. Never force either exit merely for testing. DOWN/NO entry is verified **mechanically** — orders submit, fill, and settle correctly — but is **economically suspended** as of 2026-08-13 after returning −58.0% ±14.7 per window over 44 windows; mechanical verification is not evidence of profitability and must not be read as such.
10a. Investigate Tradier, Alpaca, and IBKR against the six-step provider checklist in the SPEC open decisions. Expect most to fail at step 1 or 2 as Crypto.com, Robinhood, and ForecastEx each did; verify against official documentation rather than summaries, which have been wrong in every prior case.
11. Build the provider/variant registry and policy-identity schema before adding ForecastEx or Robinhood adapters; then add each adapter read/paper-first behind explicit capabilities. Crypto.com event contracts are withdrawn as not viable — see the Crypto.com verification note below.
11a. Market identity, per-provider tracking, per-provider budgets with market allocations, and the global-caps regression guard are done. Remaining: per-(provider, market) policy overrides for thresholds, sizing, and execution style; and candidate-set selection ordering with funding as a feasibility filter and ranking at fundable size. Defer the selection-order change until a second live-capable provider exists, since it cannot change behavior before then.
12. Add provider/live-paper/policy dashboard filters and the active-policy details/history surface.
13. **Only after validated profitability and operational evidence** consider larger stake limits, queue-aware execution rules, or any newly supported live provider.

## Operational posture

Live mode was explicitly **active** at 07:34 UTC after quiescent standalone-exit deployment, with BNB DOWN and DOGE DOWN positions, 18¢ reserved, 259¢ available, reconciliation ready, and zero blockers. Manual reconciliation confirmed both signed NO positions and recovered one fill state. The new exit engine records executable high water continuously; neither position had armed the +75% lock. No model parameter or stake limit changed. Keep stake size and second attempts unchanged while verifying the first organic strict/profit-reversal exit and monitoring re-entry, side-specific return, high-water risk, API consistency, maker-shadow, and evaluator evidence.
