---
description: Start a human-directed debugger session using the shared start-debug skill
argument-hint: '[symptom or debugging goal]'
---

Read `.agents/skills/start-debug/SKILL.md` from the current repository checkout and follow the `start-debug` skill in this session. Pass the request below as its input. If the skill is unavailable, stop and report it rather than inventing its instructions.

## Request

${ARGUMENTS:-Initialize only; no symptom or debugging goal has been supplied.}
