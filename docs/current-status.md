# Current repository status

> **Status:** Current implemented, hosted, and deployment truth
> **Host controls last verified:** 2026-09-24T05:30:00Z
> **Scope:** Volatile repository ownership, host configuration, validation evidence, and deployment gaps

This document owns current repository and host truth. Architecture documents own accepted boundaries and intent, engineering standards own command contracts, and dated evidence keeps its original as-of context. Entry points link here rather than repeating facts that can change through GitHub configuration or delivery work.

## Repository and host controls

- The GitHub Free `money-noodle` organization owns the public MIT-licensed source repository `money-noodle/money-noodle`. Protected `main` is its sole integration and delivery branch.
- Public history begins with one intentionally squashed root snapshot. The personal `phairow/money-noodle-private-archive` privately preserves development and prior-generation history; it is historical evidence, never current authority or a delivery source. The transfer preserved the public repository's immutable ID, issues, pull requests, branches, and host protections.
- Organization membership reports only the maintainer as an active organization administrator. The repository reports two write-role outside collaborators, but raw repository permission or outside-collaborator status does not make either person a maintainer-designated, eligible, independent, and available reviewer.
- GitHub Actions are enabled with read-only default workflow permissions, host-enforced full-SHA action pinning, and a selected-action allowlist that includes Trivy's pinned transitive `aquasecurity/setup-trivy` action. Private vulnerability reporting, secret scanning, and secret-scanning push protection are enabled.
- Strict `main` protection requires `affected projects and repository gates`, `secret scan`, `container platform-api`, and `container web`. It also requires one approval, stale-review dismissal, approval of the latest push, and conversation resolution, and it prohibits force pushes and deletion. Branch-protection administrator enforcement is currently disabled.
- The active default-branch `stable` ruleset separately requires pull-request controls and has an always-allowed `OrganizationAdmin` bypass actor. [`development/version-control.md#temporary-sole-maintainer-integration-exception`](development/version-control.md#temporary-sole-maintainer-integration-exception) owns the narrow stable policy for that host exception and its retirement.
- The protected `production` environment is restricted to protected branches, has only the maintainer as required reviewer, sets `prevent_self_review=true`, and reports administrator bypass capability. Read back on 2026-09-18: required reviewer `phairow` only, `prevent_self_review=true`, `can_admins_bypass=true`, deployment branch policy protected branches only. The maintainer can therefore approve an operation another actor initiated, but not one the maintainer initiated themselves. The principal's 2026-09-18 decision (#74) keeps this configuration unchanged: for a self-initiated operation the maintainer personally uses the environment administrator bypass as the recorded production consent, under the conditions in [`development/version-control.md#temporary-sole-maintainer-integration-exception`](development/version-control.md#temporary-sole-maintainer-integration-exception). No `production` job had yet reached the gate when that was read back, so both paths were configuration-derived, not observed. Since the maintainer's 2026-09-20 decision (#189) a routine deploy does not reach this environment at all: the gate applies to dispatched `apply`, `rollback`, and public-access operations only.

## Implementation, validation, and deployment

- The first-slice workspace implements pnpm/Nx projects for the Next.js web, Fastify platform API, API-owned OpenAPI document, generated TypeScript client, and separate OCI artifacts. The accepted [`architecture source and deployment map`](architecture/overview.md#source-and-deployment-map) owns the detailed paths and boundaries.
- The initial hosted `main` baseline, [run 33292553091](https://github.com/money-noodle/money-noodle/actions/runs/33292553091), passed on 2026-08-30. It exercised repository gates, OpenTofu 1.12.6 formatting, validation, and mocked tests without provider credentials, nonzero full-history Gitleaks coverage, both OCI builds, SBOM and provenance generation, and image-vulnerability scanning. This is hosted CI evidence, not provider or deployment validation.
- The platform-status behavior is implemented and locally and CI validated. Hosted CI covers affected formatting, lint, types, tests and coverage, contracts, builds, dependency and full-history secret scans, no-provider OpenTofu checks, container builds, provenance and SBOM generation, and HIGH/CRITICAL image-vulnerability gates.
- **M1 foundation is applied and live (read back 2026-09-24, #76).** A maintainer-owned Google Cloud project exists in `us-west1` with billing linked. The bootstrap stack is fully applied at `main` @ `e21074c`: a plan against its remote state reports no changes, `tofu state list` shows 42 resources, and no local state file remains. All four state buckets hold versioned remote state. The eleven repository variables of `infra/bootstrap.md` are set, and both `INFRA_APPLY_AUTHORIZED` and `PRODUCTION_ENVIRONMENT_REVIEWERS_VERIFIED` are `true`; the `authorization` job reports federation and provider as configured and the provider jobs run on `main` pushes. The repository holds zero Actions secrets and the deployer service account has zero user-managed keys; delivery authenticates only through workload-identity federation whose provider condition binds repository owner id, repository id, repository name, branch `refs/heads/main`, the `push`/`schedule`/`workflow_dispatch` events, and the exact `delivery.yml@refs/heads/main` job workflow reference. The `platform`, `api`, and `web` stacks are applied: the `platform` image registry exists, and the `platform-api` and `web` Cloud Run services exist in `us-west1` and are publicly invokable (the reviewed exposure of #182 and #184). Merge-to-main delivery runs and passes. Identifier values live only in host configuration and the maintainer's private custody, never in this repository.
- State recovery was exercised on 2026-09-24 (ADR-0006): a prior generation of the `platform` remote state (serial 2, 15 resource instances) was restored from bucket versioning into a scratch path and parsed successfully, then the scratch copy was deleted. The bootstrap-state restore path is the same bucket mechanism; the live negative federation runs of `infra/bootstrap.md` step 5 were not re-executed for this read-back and are recorded as configuration-derived evidence only.
- The principal-accepted #68 [M1 policy](operations/production-control-plane.md#m1-catalog-v2--selected-not-enabled) selects bounded catalog v2 slots, existing-repository sanitized journal/witness and restricted workload publication. These are design contracts, not installed publisher/journal/witness or qualified host/provider controls; the [staged transition](operations/delivery.md#current-to-target-activation) is still future work. Existing unapplied budget defaults are not the current [cost policy](operations/delivery.md#cost-estimates-and-operational-bounds).
- The integrated #69 runtime configuration bridge checks evaluated mocked OpenTofu output against real production readers/composition; it is provider-free source evidence, not deployed-revision or provenance proof.
- The platform has no real-money authority. Simulation and funded balances, ledgers, execution authority, presentation, and audit remain structurally separate.
